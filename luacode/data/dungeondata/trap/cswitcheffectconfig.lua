-- Decompiled using luadec 2.2 rev: 895d923 for Lua 5.3 from https://github.com/viruscamp/luadec
-- Command line: -se UTF8 luacode/data/dungeondata/trap/cswitcheffectconfig.lua 

-- params : ...
-- function num : 0 , upvalues : _ENV
local ChangeTrapStateEffect = require("logic.scene.luaevent.effect.composed.changetrapstateeffect")
local effects = {[1] = function(args)
  -- function num : 0_0 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "SwitchOff", 0.25)
  effect:SetCfgID(10100)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(1010000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [2] = function(args)
  -- function num : 0_1 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "SwitchOn", 0.25)
  effect:SetCfgID(20200)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(2020000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [3] = function(args)
  -- function num : 0_2 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(30300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
    effect:SetCfgID(30300)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(3030000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(30300)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", false)
            effect:SetCfgID(3030000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (10)", false)
            effect:SetCfgID(30300)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (10)", false)
                effect:SetCfgID(3030000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (11)", false)
                effect:SetCfgID(30300)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (11)", false)
                    effect:SetCfgID(3030000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(30303)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [4] = function(args)
  -- function num : 0_3 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(40400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(40400)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(4040000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(40400)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
            effect:SetCfgID(4040000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(40404)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [5] = function(args)
  -- function num : 0_4 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(50500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "", 0)
    effect:SetCfgID(50500)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(5050000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapCheckEffect)(args.traps, args.switches)
          effect:SetCfgID(50500)
          effect:SetParam("runoninit")
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(50505)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [6] = function(args)
  -- function num : 0_5 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(60600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 2, "", 0)
    effect:SetCfgID(60600)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 2, "", 0)
        effect:SetCfgID(6060000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapCheckEffect)(args.traps, args.switches)
          effect:SetCfgID(60600)
          effect:SetParam("runoninit")
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(60606)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [7] = function(args)
  -- function num : 0_6 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(70700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 3, "", 0)
    effect:SetCfgID(70700)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 3, "", 0)
        effect:SetCfgID(7070000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapCheckEffect)(args.traps, args.switches)
          effect:SetCfgID(70700)
          effect:SetParam("runoninit")
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(70707)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [8] = function(args)
  -- function num : 0_7 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(80800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 4, "", 0)
    effect:SetCfgID(80800)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 4, "", 0)
        effect:SetCfgID(8080000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapCheckEffect)(args.traps, args.switches)
          effect:SetCfgID(80800)
          effect:SetParam("runoninit")
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(80808)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [9] = function(args)
  -- function num : 0_8 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(90900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
    effect:SetCfgID(90900)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(9090000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(90900)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (3)", false)
            effect:SetCfgID(9090000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (12)", false)
            effect:SetCfgID(90900)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (12)", false)
                effect:SetCfgID(9090000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (13)", false)
                effect:SetCfgID(90900)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (13)", false)
                    effect:SetCfgID(9090000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(90909)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [10] = function(args)
  -- function num : 0_9 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1001000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(1001000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(100100000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(1001000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (3)", true)
            effect:SetCfgID(100100000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(10010010)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [11] = function(args)
  -- function num : 0_10 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1101100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1101100)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(110110000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(1101100)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (2)", false)
            effect:SetCfgID(110110000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(11011011)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [12] = function(args)
  -- function num : 0_11 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1201200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1201200)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(120120000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (2)", true)
        effect:SetCfgID(1201200)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (2)", true)
            effect:SetCfgID(120120000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (10)", false)
            effect:SetCfgID(1201200)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (10)", false)
                effect:SetCfgID(120120000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
                effect:SetCfgID(1201200)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
                    effect:SetCfgID(120120000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (11)", false)
                    effect:SetCfgID(1201200)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (11)", false)
                        effect:SetCfgID(120120000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)()
                            effect:SetCfgID(12012012)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [13] = function(args)
  -- function num : 0_12 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1301300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1301300)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(130130000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (4)", false)
        effect:SetCfgID(1301300)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (4)", false)
            effect:SetCfgID(130130000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
            effect:SetCfgID(1301300)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
                effect:SetCfgID(130130000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (9)", false)
                effect:SetCfgID(1301300)
                effect:SetParam("runoninit")
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (9)", false)
                    effect:SetCfgID(130130000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(13013013)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [14] = function(args)
  -- function num : 0_13 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1401400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1401400)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(140140000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (4)", true)
        effect:SetCfgID(1401400)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (4)", true)
            effect:SetCfgID(140140000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (9)", false)
            effect:SetCfgID(1401400)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (9)", false)
                effect:SetCfgID(140140000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (12)", false)
                effect:SetCfgID(1401400)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (12)", false)
                    effect:SetCfgID(140140000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (13)", false)
                    effect:SetCfgID(1401400)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (13)", false)
                        effect:SetCfgID(140140000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)()
                            effect:SetCfgID(14014014)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [15] = function(args)
  -- function num : 0_14 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1501500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1501500)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(150150000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (5)", false)
        effect:SetCfgID(1501500)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (5)", false)
            effect:SetCfgID(150150000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(15015015)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [16] = function(args)
  -- function num : 0_15 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1601600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1601600)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(160160000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (5)", true)
        effect:SetCfgID(1601600)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (5)", true)
            effect:SetCfgID(160160000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(1601600)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [17] = function(args)
  -- function num : 0_16 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1701700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1701700)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(170170000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (6)", false)
        effect:SetCfgID(1701700)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (6)", false)
            effect:SetCfgID(170170000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
            effect:SetCfgID(1701700)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
                effect:SetCfgID(170170000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (8)", false)
                effect:SetCfgID(1701700)
                effect:SetParam("runoninit")
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (8)", false)
                    effect:SetCfgID(170170000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(17017017)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [18] = function(args)
  -- function num : 0_17 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1801800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(1801800)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(180180000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (8)", false)
        effect:SetCfgID(1801800)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (8)", false)
            effect:SetCfgID(180180000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (10)", false)
            effect:SetCfgID(1801800)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (10)", false)
                effect:SetCfgID(180180000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
                effect:SetCfgID(1801800)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
                    effect:SetCfgID(180180000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (11)", false)
                    effect:SetCfgID(1801800)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (11)", false)
                        effect:SetCfgID(180180000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (14)", false)
                        effect:SetCfgID(1801800)
                        effect:SetParam("runoninit")
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (14)", false)
                            effect:SetCfgID(180180000)
                            upper:SetParam("alternative", effect)
                            root:AddEffect(effect)
                            local upper = effect
                            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (15)", false)
                            effect:SetCfgID(1801800)
                            effect:SetParam("runoninit")
                            local upper = effect
                            do
                              do
                                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (15)", false)
                                effect:SetCfgID(180180000)
                                upper:SetParam("alternative", effect)
                                root:AddEffect(effect)
                                local upper = effect
                                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (6)", true)
                                effect:SetCfgID(18018018)
                                effect:SetParam("runoninit")
                                local upper = effect
                                do
                                  do
                                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (6)", true)
                                    effect:SetCfgID(1801801800)
                                    upper:SetParam("alternative", effect)
                                    root:AddEffect(effect)
                                    local upper = effect
                                    do
                                      do
                                        local effect = (EffectFactory.CreateThawEffect)()
                                        effect:SetCfgID(18018018)
                                        root:AddEffect(effect)
                                        if upper then
                                          upper:AddEffect(root)
                                        end
                                        return composed
                                      end
                                    end
                                  end
                                end
                              end
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [19] = function(args)
  -- function num : 0_18 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1901900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1901900)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(190190000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (7)", false)
        effect:SetCfgID(1901900)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (7)", false)
            effect:SetCfgID(190190000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (14)", false)
            effect:SetCfgID(1901900)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (14)", false)
                effect:SetCfgID(190190000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (15)", false)
                effect:SetCfgID(1901900)
                effect:SetParam("runoninit")
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (15)", false)
                    effect:SetCfgID(190190000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(19019019)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [20] = function(args)
  -- function num : 0_19 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2002000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2002000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(200200000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (7)", true)
        effect:SetCfgID(2002000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (7)", true)
            effect:SetCfgID(200200000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(20020020)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [21] = function(args)
  -- function num : 0_20 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2102100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2102100)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(210210000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(2102100)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 3 4]")
              root:Add("[trap 3 2]")
              root:Add("[trap 3 3]")
              root:Add("[trap 3 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(2102100)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(21021021)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [22] = function(args)
  -- function num : 0_21 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2202200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2202200)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(220220000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(2202200)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 1 2]")
              root:Add("[trap 1 3]")
              root:Add("[trap 1 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(2202200)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(22022022)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [23] = function(args)
  -- function num : 0_22 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2302300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2302300)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(230230000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(2302300)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 1 2]")
              root:Add("[trap 1 3]")
              root:Add("[trap 1 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              local effect = (EffectFactory.CreateRemoveEventEffect)(100092)
              effect:SetCfgID(2302300)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateRemoveEventEffect)(100092)
                  effect:SetCfgID(230230000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                    effect:SetCfgID(2302300)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(23023023)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [24] = function(args)
  -- function num : 0_23 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2402400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2402400)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(240240000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(2402400)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 2 2]")
              root:Add("[trap 2 3]")
              root:Add("[trap 2 4]")
              root:Add("[trap 2 5]")
              root:Add("[trap 2 6]")
              root:Add("[trap 2 7]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(2402400)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(24024024)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [25] = function(args)
  -- function num : 0_24 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2502500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2502500)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(250250000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(2502500)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 2 2]")
              root:Add("[trap 2 3]")
              root:Add("[trap 2 4]")
              root:Add("[trap 2 5]")
              root:Add("[trap 2 6]")
              root:Add("[trap 2 7]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(2502500)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(25025025)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [26] = function(args)
  -- function num : 0_25 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2602600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2602600)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(260260000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[trap 4 2]")
            root:Add("[trap 4 3]")
            root:Add("[trap 4 4]")
            root:Add("[trap 4 5]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(26026026)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [27] = function(args)
  -- function num : 0_26 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2702700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2702700)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(270270000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[trap 4 2]")
            root:Add("[trap 4 3]")
            root:Add("[trap 4 4]")
            root:Add("[trap 4 5]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(27027027)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [28] = function(args)
  -- function num : 0_27 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2802800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2802800)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(280280000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[trap 5 2]")
            root:Add("[trap 5 3]")
            root:Add("[trap 5 4]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(28028028)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [29] = function(args)
  -- function num : 0_28 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2902900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2902900)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(290290000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[trap 5 2]")
            root:Add("[trap 5 3]")
            root:Add("[trap 5 4]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(29029029)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [30] = function(args)
  -- function num : 0_29 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3003000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3003000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(300300000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3003000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 6 2]")
              root:Add("[trap 6 3]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(3003000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(30030030)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [31] = function(args)
  -- function num : 0_30 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3103100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3103100)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(310310000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3103100)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 6 2]")
              root:Add("[trap 6 3]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(3103100)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(31031030)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [32] = function(args)
  -- function num : 0_31 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3203200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3203200)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(320320000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateBGMEffect)("play", 92)
          effect:SetCfgID(3203200)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_22", true)
          effect:SetCfgID(3203200)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_22", true)
              effect:SetCfgID(320320000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/01", false)
              effect:SetCfgID(3203200)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateVisualEffect)("EventItem/01", false)
                  effect:SetCfgID(320320000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
                    effect:SetCfgID(3203200)
                    root:AddEffect(effect)
                    local upper = root
                    local root = (ChangeTrapStateEffect.Create)()
                    local upper = root
                    do
                      do
                        local root = ((ChangeTrapStateEffect.Parallel).Create)()
                        root:Add("[trap 7 2]")
                        root:Add("[trap 7 3]")
                        root:Add("[trap 7 4]")
                        upper:Add(root)
                        upper:AddEffect(root)
                        local upper = effect
                        do
                          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                          effect:SetCfgID(3203200)
                          root:AddEffect(effect)
                          local upper = effect
                          do
                            do
                              local effect = (EffectFactory.CreateThawEffect)()
                              effect:SetCfgID(32032030)
                              root:AddEffect(effect)
                              if upper then
                                upper:AddEffect(root)
                              end
                              return composed
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [33] = function(args)
  -- function num : 0_32 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3303300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3303300)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(330330000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateBGMEffect)("play", 92)
          effect:SetCfgID(3303300)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_22", false)
          effect:SetCfgID(3303300)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_22", false)
              effect:SetCfgID(330330000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/01", true)
              effect:SetCfgID(3303300)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateVisualEffect)("EventItem/01", true)
                  effect:SetCfgID(330330000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
                    effect:SetCfgID(3303300)
                    root:AddEffect(effect)
                    local upper = root
                    local root = (ChangeTrapStateEffect.Create)()
                    local upper = root
                    do
                      do
                        local root = ((ChangeTrapStateEffect.Parallel).Create)()
                        root:Add("[trap 7 2]")
                        root:Add("[trap 7 3]")
                        root:Add("[trap 7 4]")
                        upper:Add(root)
                        upper:AddEffect(root)
                        local upper = effect
                        do
                          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                          effect:SetCfgID(3303300)
                          root:AddEffect(effect)
                          local upper = effect
                          do
                            do
                              local effect = (EffectFactory.CreateThawEffect)()
                              effect:SetCfgID(33033033)
                              root:AddEffect(effect)
                              if upper then
                                upper:AddEffect(root)
                              end
                              return composed
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [34] = function(args)
  -- function num : 0_33 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3403400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3403400)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(340340000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateBGMEffect)("play", 92)
          effect:SetCfgID(3403400)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_23", true)
          effect:SetCfgID(3403400)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_23", true)
              effect:SetCfgID(340340000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/02", false)
              effect:SetCfgID(3403400)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateVisualEffect)("EventItem/02", false)
                  effect:SetCfgID(340340000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
                    effect:SetCfgID(3403400)
                    root:AddEffect(effect)
                    local upper = root
                    local root = (ChangeTrapStateEffect.Create)()
                    local upper = root
                    do
                      do
                        local root = ((ChangeTrapStateEffect.Parallel).Create)()
                        root:Add("[trap 8 2]")
                        root:Add("[trap 8 3]")
                        root:Add("[trap 8 4]")
                        upper:Add(root)
                        upper:AddEffect(root)
                        local upper = effect
                        do
                          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                          effect:SetCfgID(3403400)
                          root:AddEffect(effect)
                          local upper = effect
                          do
                            do
                              local effect = (EffectFactory.CreateThawEffect)()
                              effect:SetCfgID(34034034)
                              root:AddEffect(effect)
                              if upper then
                                upper:AddEffect(root)
                              end
                              return composed
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [35] = function(args)
  -- function num : 0_34 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3503500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3503500)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(350350000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateBGMEffect)("play", 92)
          effect:SetCfgID(3503500)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_23", false)
          effect:SetCfgID(3503500)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_23", false)
              effect:SetCfgID(350350000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/02", true)
              effect:SetCfgID(3503500)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateVisualEffect)("EventItem/02", true)
                  effect:SetCfgID(350350000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
                    effect:SetCfgID(3503500)
                    root:AddEffect(effect)
                    local upper = root
                    local root = (ChangeTrapStateEffect.Create)()
                    local upper = root
                    do
                      do
                        local root = ((ChangeTrapStateEffect.Parallel).Create)()
                        root:Add("[trap 8 2]")
                        root:Add("[trap 8 3]")
                        root:Add("[trap 8 4]")
                        upper:Add(root)
                        upper:AddEffect(root)
                        local upper = effect
                        do
                          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                          effect:SetCfgID(3503500)
                          root:AddEffect(effect)
                          local upper = effect
                          do
                            do
                              local effect = (EffectFactory.CreateThawEffect)()
                              effect:SetCfgID(35035035)
                              root:AddEffect(effect)
                              if upper then
                                upper:AddEffect(root)
                              end
                              return composed
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [36] = function(args)
  -- function num : 0_35 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3603600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(3603600)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(360360000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(3603600)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
            effect:SetCfgID(360360000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(36036036)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [37] = function(args)
  -- function num : 0_36 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3703700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(3703700)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(370370000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(3703700)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (3)", true)
            effect:SetCfgID(370370000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(37037037)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [38] = function(args)
  -- function num : 0_37 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3803800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(3803800)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(380380000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (7)", false)
        effect:SetCfgID(3803800)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (7)", false)
            effect:SetCfgID(380380000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(38038038)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [39] = function(args)
  -- function num : 0_38 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "GoUp", 0.25)
  effect:SetCfgID(3903900)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(390390000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [40] = function(args)
  -- function num : 0_39 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "GoDown", 0.25)
  effect:SetCfgID(4004000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(400400000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [42] = function(args)
  -- function num : 0_40 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "DoorOpen", 0.1)
  effect:SetCfgID(4204200)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
      effect:SetCfgID(420420000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(3, 15, 1, 1, false)
      effect:SetCfgID(4204200)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateBlockChangeEffect)(3, 15, 1, 1, false)
          effect:SetCfgID(420420000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateRemoveEventEffect)(100627)
              effect:SetCfgID(4204200)
              effect:SetParam("runoninit")
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [43] = function(args)
  -- function num : 0_41 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "DoorOpen", 0.1)
  effect:SetCfgID(4304300)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
      effect:SetCfgID(430430000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(14, 11, 1, 1, false)
      effect:SetCfgID(4304300)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(14, 11, 1, 1, false)
            effect:SetCfgID(430430000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [44] = function(args)
  -- function num : 0_42 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "DoorClose", 0.1)
  effect:SetCfgID(4404400)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
      effect:SetCfgID(440440000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(3, 15, 1, 1, true)
      effect:SetCfgID(4404400)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(3, 15, 1, 1, true)
            effect:SetCfgID(440440000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [46] = function(args)
  -- function num : 0_43 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "DoorClose", 0.1)
  effect:SetCfgID(4604600)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
      effect:SetCfgID(460460000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(14, 11, 1, 1, true)
      effect:SetCfgID(4604600)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(14, 11, 1, 1, true)
            effect:SetCfgID(460460000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [47] = function(args)
  -- function num : 0_44 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4704700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(4704700)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(470470000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(4704700)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 3 4]")
              root:Add("[trap 3 2]")
              root:Add("[trap 3 3]")
              root:Add("[trap 3 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(4704700)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(4704700)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [48] = function(args)
  -- function num : 0_45 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4804800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(4804801)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(480480100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(4804802)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 11 2]")
              root:Add("[trap 11 3]")
              root:Add("[trap 11 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(4804804)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(4804805)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [49] = function(args)
  -- function num : 0_46 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4904900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4904901)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(490490100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(4904902)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 11 2]")
              root:Add("[trap 11 3]")
              root:Add("[trap 11 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(4904904)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(4904905)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [50] = function(args)
  -- function num : 0_47 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5005000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(5005001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(500500100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(5005002)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 12 2]")
              root:Add("[trap 12 3]")
              root:Add("[trap 12 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                effect:SetCfgID(5005004)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 12 5]")
                    root:Add("[trap 12 6]")
                    root:Add("[trap 12 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
                      effect:SetCfgID(5005006)
                      root:AddEffect(effect)
                      local upper = root
                      local root = (ChangeTrapStateEffect.Create)()
                      local upper = root
                      do
                        do
                          local root = ((ChangeTrapStateEffect.Parallel).Create)()
                          root:Add("[trap 12 8]")
                          root:Add("[trap 12 9]")
                          root:Add("[trap 12 10]")
                          upper:Add(root)
                          upper:AddEffect(root)
                          local upper = effect
                          do
                            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                            effect:SetCfgID(5005008)
                            root:AddEffect(effect)
                            local upper = effect
                            do
                              do
                                local effect = (EffectFactory.CreateThawEffect)()
                                effect:SetCfgID(5005009)
                                root:AddEffect(effect)
                                if upper then
                                  upper:AddEffect(root)
                                end
                                return composed
                              end
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [51] = function(args)
  -- function num : 0_48 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5105100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(5105101)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(510510100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(5105102)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 12 2]")
              root:Add("[trap 12 3]")
              root:Add("[trap 12 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                effect:SetCfgID(5105104)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 12 5]")
                    root:Add("[trap 12 6]")
                    root:Add("[trap 12 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
                      effect:SetCfgID(5105106)
                      root:AddEffect(effect)
                      local upper = root
                      local root = (ChangeTrapStateEffect.Create)()
                      local upper = root
                      do
                        do
                          local root = ((ChangeTrapStateEffect.Parallel).Create)()
                          root:Add("[trap 12 8]")
                          root:Add("[trap 12 9]")
                          root:Add("[trap 12 10]")
                          upper:Add(root)
                          upper:AddEffect(root)
                          local upper = effect
                          do
                            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                            effect:SetCfgID(5105108)
                            root:AddEffect(effect)
                            local upper = effect
                            do
                              do
                                local effect = (EffectFactory.CreateThawEffect)()
                                effect:SetCfgID(5105109)
                                root:AddEffect(effect)
                                if upper then
                                  upper:AddEffect(root)
                                end
                                return composed
                              end
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [52] = function(args)
  -- function num : 0_49 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5205200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(5205201)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(520520100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
          effect:SetCfgID(5205202)
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(5205203)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [53] = function(args)
  -- function num : 0_50 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5305300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(5305301)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(530530100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
          effect:SetCfgID(5305302)
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(5305303)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [54] = function(args)
  -- function num : 0_51 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5405400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(5405401)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(540540100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(5405402)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 17 3]")
              root:Add("[trap 17 4]")
              root:Add("[trap 17 5]")
              root:Add("[trap 17 7]")
              root:Add("[trap 17 8]")
              root:Add("[trap 17 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(5405404)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(5405405)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [55] = function(args)
  -- function num : 0_52 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5505500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(5505501)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(550550100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(5505502)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 17 2]")
              root:Add("[trap 17 3]")
              root:Add("[trap 17 5]")
              root:Add("[trap 17 7]")
              root:Add("[trap 17 9]")
              root:Add("[trap 17 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(5505504)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(5505505)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [56] = function(args)
  -- function num : 0_53 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5605600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 2, "", 0)
    effect:SetCfgID(5605601)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 2, "", 0)
        effect:SetCfgID(560560100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(5605602)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 17 3]")
              root:Add("[trap 17 4]")
              root:Add("[trap 17 5]")
              root:Add("[trap 17 7]")
              root:Add("[trap 17 8]")
              root:Add("[trap 17 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(5605604)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(5605605)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [57] = function(args)
  -- function num : 0_54 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5705700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 3, "", 0)
    effect:SetCfgID(5705701)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 3, "", 0)
        effect:SetCfgID(570570100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(5705702)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 17 2]")
              root:Add("[trap 17 3]")
              root:Add("[trap 17 5]")
              root:Add("[trap 17 7]")
              root:Add("[trap 17 9]")
              root:Add("[trap 17 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(5705704)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(5705705)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [62] = function(args)
  -- function num : 0_55 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6206200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(6206201)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(620620100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(6206202)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 20 2]")
              root:Add("[trap 20 3]")
              root:Add("[trap 20 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6206204)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6206205)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [63] = function(args)
  -- function num : 0_56 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6306300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(6306301)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(630630100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(6306302)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 20 2]")
              root:Add("[trap 20 3]")
              root:Add("[trap 20 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6306304)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6306305)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [64] = function(args)
  -- function num : 0_57 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6406400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(6406401)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(640640100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(6406402)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 21 2]")
              root:Add("[trap 21 3]")
              root:Add("[trap 21 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6406404)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6406405)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [65] = function(args)
  -- function num : 0_58 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6506500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(6506501)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(650650100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(6506502)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 21 2]")
              root:Add("[trap 21 3]")
              root:Add("[trap 21 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6506504)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6506505)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [66] = function(args)
  -- function num : 0_59 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6606600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(6606601)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(660660100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(6606602)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 22 2]")
              root:Add("[trap 22 3]")
              root:Add("[trap 22 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6606604)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6606605)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [67] = function(args)
  -- function num : 0_60 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6706700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(6706701)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(670670100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(6706702)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 22 2]")
              root:Add("[trap 22 3]")
              root:Add("[trap 22 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6706704)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6706705)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [68] = function(args)
  -- function num : 0_61 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6806800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(6806801)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(680680100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(6806802)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 23 2]")
              root:Add("[trap 23 3]")
              root:Add("[trap 23 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6806804)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6806805)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [69] = function(args)
  -- function num : 0_62 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6906900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(6906901)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(690690100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(6906902)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 23 2]")
              root:Add("[trap 23 3]")
              root:Add("[trap 23 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6906904)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6906905)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [70] = function(args)
  -- function num : 0_63 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7007000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(7007001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(700700100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(7007002)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 24 3]")
              root:Add("[trap 24 4]")
              root:Add("[trap 24 5]")
              root:Add("[trap 24 7]")
              root:Add("[trap 24 8]")
              root:Add("[trap 24 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7007004)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7007005)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [71] = function(args)
  -- function num : 0_64 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7107100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(7107101)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(710710100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(7107102)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 24 2]")
              root:Add("[trap 24 3]")
              root:Add("[trap 24 5]")
              root:Add("[trap 24 7]")
              root:Add("[trap 24 9]")
              root:Add("[trap 24 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7107104)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7107105)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [72] = function(args)
  -- function num : 0_65 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7207200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 2, "", 0)
    effect:SetCfgID(7207201)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 2, "", 0)
        effect:SetCfgID(720720100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(7207202)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 24 3]")
              root:Add("[trap 24 4]")
              root:Add("[trap 24 5]")
              root:Add("[trap 24 7]")
              root:Add("[trap 24 8]")
              root:Add("[trap 24 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7207204)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7207205)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [73] = function(args)
  -- function num : 0_66 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7307300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 3, "", 0)
    effect:SetCfgID(7307301)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 3, "", 0)
        effect:SetCfgID(730730100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(7307302)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 24 2]")
              root:Add("[trap 24 3]")
              root:Add("[trap 24 5]")
              root:Add("[trap 24 7]")
              root:Add("[trap 24 9]")
              root:Add("[trap 24 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7307304)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7307305)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [74] = function(args)
  -- function num : 0_67 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7407400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(7407401)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(740740100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(7407402)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 25 2]")
              root:Add("[trap 25 3]")
              root:Add("[trap 25 4]")
              root:Add("[trap 25 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7407404)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7407405)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [75] = function(args)
  -- function num : 0_68 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7507500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(7507501)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(750750100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(7507502)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 25 2]")
              root:Add("[trap 25 3]")
              root:Add("[trap 25 4]")
              root:Add("[trap 25 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7507504)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7507505)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [76] = function(args)
  -- function num : 0_69 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7607600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(7607601)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(760760100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(7607602)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 26 2]")
              root:Add("[trap 26 3]")
              root:Add("[trap 26 4]")
              root:Add("[trap 26 5]")
              root:Add("[trap 26 6]")
              root:Add("[trap 26 7]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(7607604)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 26 8]")
                    root:Add("[trap 26 9]")
                    root:Add("[trap 26 10]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(7607606)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(7607607)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [77] = function(args)
  -- function num : 0_70 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7707700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(7707701)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(770770100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(7707702)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 26 2]")
              root:Add("[trap 26 3]")
              root:Add("[trap 26 4]")
              root:Add("[trap 26 5]")
              root:Add("[trap 26 6]")
              root:Add("[trap 26 7]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(7707704)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 26 8]")
                    root:Add("[trap 26 9]")
                    root:Add("[trap 26 10]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(7707706)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(7707707)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [78] = function(args)
  -- function num : 0_71 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7807800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(7807801)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(780780100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(7807802)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 27 3]")
              root:Add("[trap 27 4]")
              root:Add("[trap 27 5]")
              root:Add("[trap 27 7]")
              root:Add("[trap 27 8]")
              root:Add("[trap 27 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7807804)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7807805)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [79] = function(args)
  -- function num : 0_72 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7907900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(7907901)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(790790100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(7907902)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 27 2]")
              root:Add("[trap 27 3]")
              root:Add("[trap 27 5]")
              root:Add("[trap 27 7]")
              root:Add("[trap 27 9]")
              root:Add("[trap 27 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7907904)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7907905)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [80] = function(args)
  -- function num : 0_73 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8008000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(8008001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(800800100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(8008002)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 28 2]")
              root:Add("[trap 28 3]")
              root:Add("[trap 28 4]")
              root:Add("[trap 28 5]")
              root:Add("[trap 28 6]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(8008004)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 28 7]")
                    root:Add("[trap 28 8]")
                    root:Add("[trap 28 9]")
                    root:Add("[trap 28 10]")
                    root:Add("[trap 28 11]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(8008006)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(8008007)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [81] = function(args)
  -- function num : 0_74 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8108100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(8108101)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(810810100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(8108102)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 28 2]")
              root:Add("[trap 28 3]")
              root:Add("[trap 28 4]")
              root:Add("[trap 28 5]")
              root:Add("[trap 28 6]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(8108104)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 28 7]")
                    root:Add("[trap 28 8]")
                    root:Add("[trap 28 9]")
                    root:Add("[trap 28 10]")
                    root:Add("[trap 28 11]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(8108106)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(8108107)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [82] = function(args)
  -- function num : 0_75 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8208200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(8208201)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(820820100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(8208202)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 29 2]")
              root:Add("[trap 29 3]")
              root:Add("[trap 29 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
                effect:SetCfgID(8208204)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 29 5]")
                    root:Add("[trap 29 6]")
                    root:Add("[trap 29 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(8208206)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(8208207)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [83] = function(args)
  -- function num : 0_76 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8308300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(8308301)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(830830100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(8308302)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 29 2]")
              root:Add("[trap 29 3]")
              root:Add("[trap 29 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
                effect:SetCfgID(8308304)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 29 5]")
                    root:Add("[trap 29 6]")
                    root:Add("[trap 29 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(8308306)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(8308307)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [84] = function(args)
  -- function num : 0_77 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8408400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(8408401)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(840840100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(8408402)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 32 2]")
              root:Add("[trap 32 3]")
              root:Add("[trap 32 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(8408404)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(8408405)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [85] = function(args)
  -- function num : 0_78 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8508500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(8508501)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(850850100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(8508502)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 32 2]")
              root:Add("[trap 32 3]")
              root:Add("[trap 32 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(8508504)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(8508505)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [86] = function(args)
  -- function num : 0_79 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8608600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(8608601)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(860860100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(8608602)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 33 2]")
              root:Add("[trap 33 3]")
              root:Add("[trap 33 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                effect:SetCfgID(8608604)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 33 5]")
                    root:Add("[trap 33 6]")
                    root:Add("[trap 33 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(8608606)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(8608607)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [87] = function(args)
  -- function num : 0_80 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8708700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(8708701)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(870870100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(8708702)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 33 2]")
              root:Add("[trap 33 3]")
              root:Add("[trap 33 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                effect:SetCfgID(8708704)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 33 5]")
                    root:Add("[trap 33 6]")
                    root:Add("[trap 33 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(8708706)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(8708707)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [88] = function(args)
  -- function num : 0_81 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8808800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(8808801)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(880880100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(8808802)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 34 2]")
              root:Add("[trap 34 3]")
              root:Add("[trap 34 4]")
              root:Add("[trap 34 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(8808804)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(8808805)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [89] = function(args)
  -- function num : 0_82 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8908900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(8908901)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(890890100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(8908902)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 34 2]")
              root:Add("[trap 34 3]")
              root:Add("[trap 34 4]")
              root:Add("[trap 34 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(8908904)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(8908905)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [90] = function(args)
  -- function num : 0_83 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9009000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(9009001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(900900100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(9009002)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(9009003)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(9009004)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(9009005)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [91] = function(args)
  -- function num : 0_84 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9109100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0.25)
    effect:SetCfgID(9109101)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(910910100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(9109102)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(9109103)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(9109104)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(9109105)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [92] = function(args)
  -- function num : 0_85 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
      effect:SetCfgID(9209201)
      root:AddEffect(effect)
      if upper then
        upper:AddEffect(root)
      end
      return composed
    end
  end
end
, [93] = function(args)
  -- function num : 0_86 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
      effect:SetCfgID(9309301)
      root:AddEffect(effect)
      if upper then
        upper:AddEffect(root)
      end
      return composed
    end
  end
end
, [94] = function(args)
  -- function num : 0_87 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9409400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 2, "", 0)
    effect:SetCfgID(9409401)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 2, "", 0)
        effect:SetCfgID(940940100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(9409402)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 27 3]")
              root:Add("[trap 27 4]")
              root:Add("[trap 27 5]")
              root:Add("[trap 27 7]")
              root:Add("[trap 27 8]")
              root:Add("[trap 27 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(9409404)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(9409405)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [95] = function(args)
  -- function num : 0_88 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9509500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 3, "", 0)
    effect:SetCfgID(9509501)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 3, "", 0)
        effect:SetCfgID(950950100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(9509502)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 27 2]")
              root:Add("[trap 27 3]")
              root:Add("[trap 27 5]")
              root:Add("[trap 27 7]")
              root:Add("[trap 27 9]")
              root:Add("[trap 27 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(9509504)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(9509505)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [96] = function(args)
  -- function num : 0_89 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9609600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "SwitchState", 1, "", 0)
    effect:SetCfgID(9609600)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "loadState", 1, "", 0)
        effect:SetCfgID(960960000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(9609600)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(9609600)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(9609600)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(9609600)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [97] = function(args)
  -- function num : 0_90 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9709700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "SwitchState", 0, "", 0)
    effect:SetCfgID(9709700)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "loadState", 0, "", 0)
        effect:SetCfgID(970970000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(9709700)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(9709700)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(9709700)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(9709700)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [98] = function(args)
  -- function num : 0_91 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9809800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "SwitchState", 1, "", 0)
    effect:SetCfgID(9809800)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "loadState", 1, "", 0)
        effect:SetCfgID(980980000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(9809800)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(9809800)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(9809800)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(9809800)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [99] = function(args)
  -- function num : 0_92 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9909900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "SwitchState", 0, "", 0)
    effect:SetCfgID(9909900)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "loadState", 0, "", 0)
        effect:SetCfgID(990990000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(9909900)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(9909900)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(9909900)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(9909900)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [100] = function(args)
  -- function num : 0_93 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1000000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1000000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(100000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1000000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1000000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1000000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1000000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [101] = function(args)
  -- function num : 0_94 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1010000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1010000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(101000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1010000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1010000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1010000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1010000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [102] = function(args)
  -- function num : 0_95 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1020000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1020000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(102000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1020000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 53 2]")
              root:Add("[trap 53 3]")
              root:Add("[trap 53 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1020000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 53 5]")
                    root:Add("[trap 53 6]")
                    root:Add("[trap 53 7]")
                    root:Add("[trap 53 8]")
                    root:Add("[trap 53 9]")
                    root:Add("[trap 53 10]")
                    root:Add("[trap 53 11]")
                    root:Add("[trap 53 12]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1020000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1020000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [103] = function(args)
  -- function num : 0_96 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1030000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1030000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(103000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1030000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 53 2]")
              root:Add("[trap 53 3]")
              root:Add("[trap 53 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1030000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 53 5]")
                    root:Add("[trap 53 6]")
                    root:Add("[trap 53 7]")
                    root:Add("[trap 53 8]")
                    root:Add("[trap 53 9]")
                    root:Add("[trap 53 10]")
                    root:Add("[trap 53 11]")
                    root:Add("[trap 53 12]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1030000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1030000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [104] = function(args)
  -- function num : 0_97 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1040000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1040000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(104000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(1040000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1040000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1040000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1040000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [105] = function(args)
  -- function num : 0_98 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1050000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1050000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(105000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(1050000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1050000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1050000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1050000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [106] = function(args)
  -- function num : 0_99 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1060000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1060000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(106000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1060000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 55 2]")
              root:Add("[trap 55 3]")
              root:Add("[trap 55 4]")
              root:Add("[trap 55 5]")
              root:Add("[trap 55 6]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1060000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 55 7]")
                    root:Add("[trap 55 8]")
                    root:Add("[trap 55 9]")
                    root:Add("[trap 55 10]")
                    root:Add("[trap 55 11]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1060000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1060000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [107] = function(args)
  -- function num : 0_100 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1070000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1070000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(107000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1070000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 55 2]")
              root:Add("[trap 55 3]")
              root:Add("[trap 55 4]")
              root:Add("[trap 55 5]")
              root:Add("[trap 55 6]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1070000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 55 7]")
                    root:Add("[trap 55 8]")
                    root:Add("[trap 55 9]")
                    root:Add("[trap 55 10]")
                    root:Add("[trap 55 11]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1070000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1070000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [108] = function(args)
  -- function num : 0_101 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1080000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1080000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(108000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1080000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 56 2]")
              root:Add("[trap 56 3]")
              root:Add("[trap 56 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1080000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 56 5]")
                    root:Add("[trap 56 6]")
                    root:Add("[trap 56 7]")
                    root:Add("[trap 56 8]")
                    root:Add("[trap 56 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1080000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1080000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [109] = function(args)
  -- function num : 0_102 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1090000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1090000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(109000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1090000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 56 2]")
              root:Add("[trap 56 3]")
              root:Add("[trap 56 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1090000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 56 5]")
                    root:Add("[trap 56 6]")
                    root:Add("[trap 56 7]")
                    root:Add("[trap 56 8]")
                    root:Add("[trap 56 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1090000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1090000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [110] = function(args)
  -- function num : 0_103 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1100000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1100001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(110000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1100002)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1100003)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1100004)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1100005)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [111] = function(args)
  -- function num : 0_104 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1110000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1110001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(111000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1110002)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1110003)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1110004)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1110005)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [112] = function(args)
  -- function num : 0_105 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1120000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1120001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(112000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1120004)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 58 5]")
              root:Add("[trap 58 6]")
              root:Add("[trap 58 7]")
              root:Add("[trap 58 8]")
              root:Add("[trap 58 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM08")
                effect:SetCfgID(1120006)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 58 10]")
                    root:Add("[trap 58 11]")
                    root:Add("[trap 58 12]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1120008)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1120009)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [113] = function(args)
  -- function num : 0_106 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1130000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1130001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(113000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1130004)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 58 5]")
              root:Add("[trap 58 6]")
              root:Add("[trap 58 7]")
              root:Add("[trap 58 8]")
              root:Add("[trap 58 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM08")
                effect:SetCfgID(1130006)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 58 10]")
                    root:Add("[trap 58 11]")
                    root:Add("[trap 58 12]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1130008)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1130009)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [114] = function(args)
  -- function num : 0_107 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1140000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1140001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(114000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(1140002)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 59 2]")
              root:Add("[trap 59 3]")
              root:Add("[trap 59 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
                effect:SetCfgID(1140004)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 59 5]")
                    root:Add("[trap 59 6]")
                    root:Add("[trap 59 7]")
                    root:Add("[trap 59 8]")
                    root:Add("[trap 59 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1140008)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1140009)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [115] = function(args)
  -- function num : 0_108 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1150000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1150001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(115000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(1150002)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 59 2]")
              root:Add("[trap 59 3]")
              root:Add("[trap 59 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
                effect:SetCfgID(1150004)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 59 5]")
                    root:Add("[trap 59 6]")
                    root:Add("[trap 59 7]")
                    root:Add("[trap 59 8]")
                    root:Add("[trap 59 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1150008)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1150009)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [116] = function(args)
  -- function num : 0_109 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1160000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1160000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(116000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1160000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1160000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1160000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1160000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [117] = function(args)
  -- function num : 0_110 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1170000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1170000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(117000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1170000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1170000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1170000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1170000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [118] = function(args)
  -- function num : 0_111 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1180000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1180000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(118000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1180000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 61 2]")
              root:Add("[trap 61 3]")
              root:Add("[trap 61 4]")
              root:Add("[trap 61 5]")
              root:Add("[trap 61 6]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(1180000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 61 7]")
                    root:Add("[trap 61 8]")
                    root:Add("[trap 61 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1180000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1180000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [119] = function(args)
  -- function num : 0_112 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1190000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1190000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(119000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1190000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 61 2]")
              root:Add("[trap 61 3]")
              root:Add("[trap 61 4]")
              root:Add("[trap 61 5]")
              root:Add("[trap 61 6]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(1190000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 61 7]")
                    root:Add("[trap 61 8]")
                    root:Add("[trap 61 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1190000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1190000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [120] = function(args)
  -- function num : 0_113 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1200000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1200000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(120000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1200000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1200000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1200000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1200000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [121] = function(args)
  -- function num : 0_114 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1210000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1210000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(121000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1210000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1210000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1210000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1210000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [122] = function(args)
  -- function num : 0_115 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1220000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1220000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0.25)
        effect:SetCfgID(122000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1220000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 63 3]")
              root:Add("[trap 63 4]")
              root:Add("[trap 63 5]")
              root:Add("[trap 63 7]")
              root:Add("[trap 63 8]")
              root:Add("[trap 63 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1220000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1220000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [123] = function(args)
  -- function num : 0_116 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1230000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0.25)
    effect:SetCfgID(1230000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0.25)
        effect:SetCfgID(123000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1230000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 63 2]")
              root:Add("[trap 63 3]")
              root:Add("[trap 63 5]")
              root:Add("[trap 63 7]")
              root:Add("[trap 63 9]")
              root:Add("[trap 63 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1230000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1230000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [124] = function(args)
  -- function num : 0_117 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1240000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 2, "", 0)
    effect:SetCfgID(1240000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 2, "", 0)
        effect:SetCfgID(124000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1240000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 63 3]")
              root:Add("[trap 63 4]")
              root:Add("[trap 63 5]")
              root:Add("[trap 63 7]")
              root:Add("[trap 63 8]")
              root:Add("[trap 63 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1240000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1240000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [125] = function(args)
  -- function num : 0_118 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1250000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 3, "", 0)
    effect:SetCfgID(1250000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 3, "", 0)
        effect:SetCfgID(125000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1250000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 63 2]")
              root:Add("[trap 63 3]")
              root:Add("[trap 63 5]")
              root:Add("[trap 63 7]")
              root:Add("[trap 63 9]")
              root:Add("[trap 63 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1250000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1250000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [126] = function(args)
  -- function num : 0_119 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1260000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1260000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(126000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1260000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1260000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1260000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1260000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [127] = function(args)
  -- function num : 0_120 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1270000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1270000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(127000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1270000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1270000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1270000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1270000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [128] = function(args)
  -- function num : 0_121 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1280000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1280000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(128000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1280000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1280000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1280000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1280000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [129] = function(args)
  -- function num : 0_122 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1290000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1290000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(129000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1290000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1290000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1290000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1290000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [130] = function(args)
  -- function num : 0_123 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1300000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1300000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0.25)
        effect:SetCfgID(130000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1300000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 66 3]")
              root:Add("[trap 66 4]")
              root:Add("[trap 66 5]")
              root:Add("[trap 66 7]")
              root:Add("[trap 66 8]")
              root:Add("[trap 66 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1300000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1300000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [131] = function(args)
  -- function num : 0_124 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1310000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0.25)
    effect:SetCfgID(1310000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0.25)
        effect:SetCfgID(131000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1310000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 66 2]")
              root:Add("[trap 66 3]")
              root:Add("[trap 66 5]")
              root:Add("[trap 66 7]")
              root:Add("[trap 66 9]")
              root:Add("[trap 66 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1310000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1310000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [132] = function(args)
  -- function num : 0_125 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1320000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 2, "", 0.25)
    effect:SetCfgID(1320000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 2, "", 0.25)
        effect:SetCfgID(132000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1320000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 66 3]")
              root:Add("[trap 66 4]")
              root:Add("[trap 66 5]")
              root:Add("[trap 66 7]")
              root:Add("[trap 66 8]")
              root:Add("[trap 66 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1320000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1320000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [133] = function(args)
  -- function num : 0_126 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1330000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 3, "", 0.25)
    effect:SetCfgID(1330000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 3, "", 0.25)
        effect:SetCfgID(133000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1330000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 66 2]")
              root:Add("[trap 66 3]")
              root:Add("[trap 66 5]")
              root:Add("[trap 66 7]")
              root:Add("[trap 66 9]")
              root:Add("[trap 66 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1330000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1330000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [134] = function(args)
  -- function num : 0_127 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1340000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1340000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(134000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1340000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1340000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1340000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1340000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [135] = function(args)
  -- function num : 0_128 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1350000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1350000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(135000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1350000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1350000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1350000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1350000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [136] = function(args)
  -- function num : 0_129 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1360000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1360000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(136000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1360000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 68 2]")
              root:Add("[trap 68 3]")
              root:Add("[trap 68 4]")
              root:Add("[trap 68 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(1360000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 68 6]")
                    root:Add("[trap 68 7]")
                    root:Add("[trap 68 8]")
                    root:Add("[trap 68 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1360000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1360000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [137] = function(args)
  -- function num : 0_130 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1370000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1370000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(137000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1370000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 68 2]")
              root:Add("[trap 68 3]")
              root:Add("[trap 68 4]")
              root:Add("[trap 68 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(1370000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 68 6]")
                    root:Add("[trap 68 7]")
                    root:Add("[trap 68 8]")
                    root:Add("[trap 68 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1370000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1370000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [138] = function(args)
  -- function num : 0_131 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1380000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1380000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(138000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1380000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1380000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1380000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1380000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [139] = function(args)
  -- function num : 0_132 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1390000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1390000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(139000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1390000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1390000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1390000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1390000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [140] = function(args)
  -- function num : 0_133 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1400000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1400000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(140000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1400000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1400000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1400000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1400000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [141] = function(args)
  -- function num : 0_134 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1410000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1410000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(141000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1410000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1410000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1410000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1410000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [142] = function(args)
  -- function num : 0_135 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1420000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1420000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(142000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[trap 71 5]")
            root:Add("[trap 71 6]")
            root:Add("[trap 71 7]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
              effect:SetCfgID(1420000)
              root:AddEffect(effect)
              local upper = root
              local root = (ChangeTrapStateEffect.Create)()
              local upper = root
              do
                do
                  local root = ((ChangeTrapStateEffect.Parallel).Create)()
                  root:Add("[trap 71 2]")
                  root:Add("[trap 71 3]")
                  root:Add("[trap 71 4]")
                  upper:Add(root)
                  upper:AddEffect(root)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                    effect:SetCfgID(1420000)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(1420000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [143] = function(args)
  -- function num : 0_136 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1430000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1430000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(143000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[trap 71 5]")
            root:Add("[trap 71 6]")
            root:Add("[trap 71 7]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
              effect:SetCfgID(1430000)
              root:AddEffect(effect)
              local upper = root
              local root = (ChangeTrapStateEffect.Create)()
              local upper = root
              do
                do
                  local root = ((ChangeTrapStateEffect.Parallel).Create)()
                  root:Add("[trap 71 2]")
                  root:Add("[trap 71 3]")
                  root:Add("[trap 71 4]")
                  upper:Add(root)
                  upper:AddEffect(root)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                    effect:SetCfgID(1430000)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(1430000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [144] = function(args)
  -- function num : 0_137 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1440000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1440000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(144000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1440000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1440000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1440000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1440000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [145] = function(args)
  -- function num : 0_138 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1450000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1450000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(145000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1450000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1450000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1450000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1450000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [146] = function(args)
  -- function num : 0_139 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1460000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1460000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(146000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1460000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 73 2]")
              root:Add("[trap 73 3]")
              root:Add("[trap 73 4]")
              root:Add("[trap 73 8]")
              root:Add("[trap 73 9]")
              root:Add("[trap 73 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(1460000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 73 5]")
                    root:Add("[trap 73 6]")
                    root:Add("[trap 73 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1460000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1460000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [147] = function(args)
  -- function num : 0_140 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1470000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1470000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(147000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1470000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 73 2]")
              root:Add("[trap 73 3]")
              root:Add("[trap 73 4]")
              root:Add("[trap 73 8]")
              root:Add("[trap 73 9]")
              root:Add("[trap 73 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(1470000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 73 5]")
                    root:Add("[trap 73 6]")
                    root:Add("[trap 73 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1470000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1470000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [148] = function(args)
  -- function num : 0_141 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1480000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1480000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(148000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1480000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 74 2]")
              root:Add("[trap 74 3]")
              root:Add("[trap 74 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1480000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 74 5]")
                    root:Add("[trap 74 6]")
                    root:Add("[trap 74 7]")
                    root:Add("[trap 74 8]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1480000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1480000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [149] = function(args)
  -- function num : 0_142 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1490000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1490000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(149000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1490000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 74 2]")
              root:Add("[trap 74 3]")
              root:Add("[trap 74 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1490000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 74 5]")
                    root:Add("[trap 74 6]")
                    root:Add("[trap 74 7]")
                    root:Add("[trap 74 8]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1490000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1490000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [150] = function(args)
  -- function num : 0_143 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1500000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1500000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(150000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1500000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1500000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1500000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1500000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [151] = function(args)
  -- function num : 0_144 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1510000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1510000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(151000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1510000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1510000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1510000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1510000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [152] = function(args)
  -- function num : 0_145 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "SkateRecover", 0.25)
  effect:SetCfgID(1520001)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(152000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [153] = function(args)
  -- function num : 0_146 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "SkateBreak", 0.25)
  effect:SetCfgID(1530001)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(153000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [154] = function(args)
  -- function num : 0_147 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1540000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1540000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(154000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1540000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1540000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1540000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1540000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [155] = function(args)
  -- function num : 0_148 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1550000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1550000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(155000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1550000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1550000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1550000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1550000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [156] = function(args)
  -- function num : 0_149 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1560000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1560000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(156000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1560000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1560000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1560000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1560000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [157] = function(args)
  -- function num : 0_150 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1570000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1570000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(157000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1570000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1570000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1570000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1570000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [158] = function(args)
  -- function num : 0_151 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1580000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1580000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(158000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1580000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1580000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1580000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1580000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [159] = function(args)
  -- function num : 0_152 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1590000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1590000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(159000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1590000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1590000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1590000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1590000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [160] = function(args)
  -- function num : 0_153 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1600100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1600100)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(160010000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
          effect:SetCfgID(1600100)
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(1600100)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [161] = function(args)
  -- function num : 0_154 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1610200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1610200)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(161020000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
          effect:SetCfgID(1610200)
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(1610200)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [162] = function(args)
  -- function num : 0_155 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1620000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1620000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(162000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1620000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1620000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1620000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1620000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [163] = function(args)
  -- function num : 0_156 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1630000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1630000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(163000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1630000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1630000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1630000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1630000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [164] = function(args)
  -- function num : 0_157 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "SwitchOff", 0.25)
  effect:SetCfgID(1640100)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(164010000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [165] = function(args)
  -- function num : 0_158 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "SwitchOn", 0.25)
  effect:SetCfgID(1650200)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(165020000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [166] = function(args)
  -- function num : 0_159 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1660000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1660000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(166000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1660000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1660000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1660000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1660000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [167] = function(args)
  -- function num : 0_160 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1670000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1670000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(167000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1670000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1670000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1670000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1670000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [168] = function(args)
  -- function num : 0_161 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1680000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1680000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(168000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1680000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1680000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1680000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1680000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [169] = function(args)
  -- function num : 0_162 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1690000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1690000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(169000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1690000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1690000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1690000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1690000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [170] = function(args)
  -- function num : 0_163 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1700000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1700000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(170000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1700000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 98 2]")
              root:Add("[trap 98 3]")
              root:Add("[trap 98 4]")
              root:Add("[trap 98 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1700000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 98 6]")
                    root:Add("[trap 98 7]")
                    root:Add("[trap 98 8]")
                    root:Add("[trap 98 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1700000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1700000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [171] = function(args)
  -- function num : 0_164 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1710000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1710000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(171000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1710000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 98 2]")
              root:Add("[trap 98 3]")
              root:Add("[trap 98 4]")
              root:Add("[trap 98 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1710000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 98 6]")
                    root:Add("[trap 98 7]")
                    root:Add("[trap 98 8]")
                    root:Add("[trap 98 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1710000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1710000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [172] = function(args)
  -- function num : 0_165 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1720000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1720000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(172000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1720000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1720000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1720000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1720000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [173] = function(args)
  -- function num : 0_166 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1730000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1730000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(173000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1730000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1730000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1730000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1730000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [174] = function(args)
  -- function num : 0_167 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1740000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1740000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(174000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1740000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1740000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1740000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1740000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [175] = function(args)
  -- function num : 0_168 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1750000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1750000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(175000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1750000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1750000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1750000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1750000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [176] = function(args)
  -- function num : 0_169 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1760000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1760000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(176000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1760000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1760000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1760000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1760000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [177] = function(args)
  -- function num : 0_170 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1770000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1770000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(177000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1770000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1770000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1770000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1770000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [178] = function(args)
  -- function num : 0_171 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1780000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1780000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(178000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1780000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1780000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1780000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1780000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [179] = function(args)
  -- function num : 0_172 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1790000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1790000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(179000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1790000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1790000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1790000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1790000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [180] = function(args)
  -- function num : 0_173 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1800000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1800000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(180000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1800000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1800000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1800000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1800000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [181] = function(args)
  -- function num : 0_174 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1810000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1810000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(181000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1810000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1810000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1810000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1810000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [182] = function(args)
  -- function num : 0_175 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1820000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1820000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(182000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1820000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1820000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1820000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1820000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [183] = function(args)
  -- function num : 0_176 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1830000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1830000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(183000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1830000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1830000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1830000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1830000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [184] = function(args)
  -- function num : 0_177 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1840000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "switchState", 0, "", 0)
      effect:SetCfgID(1840000)
      effect:SetParam("runoninit")
      root:AddEffect(effect)
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateThawEffect)()
          effect:SetCfgID(1840000)
          root:AddEffect(effect)
          if upper then
            upper:AddEffect(root)
          end
          return composed
        end
      end
    end
  end
end
, [185] = function(args)
  -- function num : 0_178 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1850000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "switchState", 1, "", 0)
      effect:SetCfgID(1850000)
      effect:SetParam("runoninit")
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
        effect:SetCfgID(1850000)
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateThawEffect)()
            effect:SetCfgID(1850000)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [186] = function(args)
  -- function num : 0_179 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
      effect:SetCfgID(1860000)
      effect:SetParam("runoninit")
      root:AddEffect(effect)
      if upper then
        upper:AddEffect(root)
      end
      return composed
    end
  end
end
, [187] = function(args)
  -- function num : 0_180 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
      effect:SetCfgID(1870000)
      effect:SetParam("runoninit")
      root:AddEffect(effect)
      if upper then
        upper:AddEffect(root)
      end
      return composed
    end
  end
end
, [188] = function(args)
  -- function num : 0_181 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
      effect:SetCfgID(1880000)
      effect:SetParam("runoninit")
      root:AddEffect(effect)
      if upper then
        upper:AddEffect(root)
      end
      return composed
    end
  end
end
, [189] = function(args)
  -- function num : 0_182 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 1, "TowerOn4", 1)
      effect:SetCfgID(1890000)
      effect:SetParam("runoninit")
      root:AddEffect(effect)
      if upper then
        upper:AddEffect(root)
      end
      return composed
    end
  end
end
, [190] = function(args)
  -- function num : 0_183 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(15, 15, 3, 1, true)
  effect:SetCfgID(1900000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(15, 15, 3, 1, true)
      effect:SetCfgID(190000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
      effect:SetCfgID(1900000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(190000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(1900000)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
              effect:SetCfgID(190000000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
              effect:SetCfgID(1900000)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
                  effect:SetCfgID(190000000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                  effect:SetCfgID(1900000)
                  effect:SetParam("runoninit")
                  local upper = effect
                  do
                    do
                      do
                        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                        effect:SetCfgID(190000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [191] = function(args)
  -- function num : 0_184 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1910000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1910000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(191000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1910000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 112 2]")
              root:Add("[trap 112 3]")
              root:Add("[trap 112 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1910000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 112 5]")
                    root:Add("[trap 112 6]")
                    root:Add("[trap 112 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                      effect:SetCfgID(1910000)
                      root:AddEffect(effect)
                      local upper = root
                      local root = (ChangeTrapStateEffect.Create)()
                      local upper = root
                      do
                        do
                          local root = ((ChangeTrapStateEffect.Parallel).Create)()
                          root:Add("[trap 112 8]")
                          root:Add("[trap 112 9]")
                          root:Add("[trap 112 10]")
                          root:Add("[trap 112 11]")
                          upper:Add(root)
                          upper:AddEffect(root)
                          local upper = effect
                          do
                            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
                            effect:SetCfgID(1910000)
                            root:AddEffect(effect)
                            local upper = root
                            local root = (ChangeTrapStateEffect.Create)()
                            local upper = root
                            do
                              do
                                local root = ((ChangeTrapStateEffect.Parallel).Create)()
                                root:Add("[trap 112 12]")
                                root:Add("[trap 112 13]")
                                root:Add("[trap 112 14]")
                                root:Add("[trap 112 15]")
                                upper:Add(root)
                                upper:AddEffect(root)
                                local upper = effect
                                do
                                  local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                                  effect:SetCfgID(1910000)
                                  root:AddEffect(effect)
                                  local upper = effect
                                  do
                                    do
                                      local effect = (EffectFactory.CreateThawEffect)()
                                      effect:SetCfgID(1910000)
                                      root:AddEffect(effect)
                                      if upper then
                                        upper:AddEffect(root)
                                      end
                                      return composed
                                    end
                                  end
                                end
                              end
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [192] = function(args)
  -- function num : 0_185 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1920000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1920000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(192000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1920000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 112 2]")
              root:Add("[trap 112 3]")
              root:Add("[trap 112 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1920000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 112 5]")
                    root:Add("[trap 112 6]")
                    root:Add("[trap 112 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                      effect:SetCfgID(1920000)
                      root:AddEffect(effect)
                      local upper = root
                      local root = (ChangeTrapStateEffect.Create)()
                      local upper = root
                      do
                        do
                          local root = ((ChangeTrapStateEffect.Parallel).Create)()
                          root:Add("[trap 112 8]")
                          root:Add("[trap 112 9]")
                          root:Add("[trap 112 10]")
                          root:Add("[trap 112 11]")
                          upper:Add(root)
                          upper:AddEffect(root)
                          local upper = effect
                          do
                            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
                            effect:SetCfgID(1920000)
                            root:AddEffect(effect)
                            local upper = root
                            local root = (ChangeTrapStateEffect.Create)()
                            local upper = root
                            do
                              do
                                local root = ((ChangeTrapStateEffect.Parallel).Create)()
                                root:Add("[trap 112 12]")
                                root:Add("[trap 112 13]")
                                root:Add("[trap 112 14]")
                                root:Add("[trap 112 15]")
                                upper:Add(root)
                                upper:AddEffect(root)
                                local upper = effect
                                do
                                  local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                                  effect:SetCfgID(1920000)
                                  root:AddEffect(effect)
                                  local upper = effect
                                  do
                                    do
                                      local effect = (EffectFactory.CreateThawEffect)()
                                      effect:SetCfgID(1920000)
                                      root:AddEffect(effect)
                                      if upper then
                                        upper:AddEffect(root)
                                      end
                                      return composed
                                    end
                                  end
                                end
                              end
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [193] = function(args)
  -- function num : 0_186 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1930000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1930000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(193000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1930000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1930000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1930000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1930000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [194] = function(args)
  -- function num : 0_187 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1940000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1940000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(194000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1940000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1940000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1940000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1940000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [195] = function(args)
  -- function num : 0_188 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1950000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1950000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(195000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM08")
          effect:SetCfgID(1950000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1950000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1950000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1950000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [196] = function(args)
  -- function num : 0_189 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1960000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1960000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(196000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM08")
          effect:SetCfgID(1960000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1960000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1960000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1960000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [197] = function(args)
  -- function num : 0_190 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1970000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1970000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(197000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1970000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1970000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1970000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1970000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [198] = function(args)
  -- function num : 0_191 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1980000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1980000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(198000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1980000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1980000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1980000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1980000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [199] = function(args)
  -- function num : 0_192 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1990000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1990000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(199000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1990000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1990000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1990000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1990000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [200] = function(args)
  -- function num : 0_193 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2000000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2000000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(200000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(2000000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(2000000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(2000000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(2000000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [201] = function(args)
  -- function num : 0_194 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2010000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "switchState", 0, "", 0)
      effect:SetCfgID(2010000)
      root:AddEffect(effect)
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateThawEffect)()
          effect:SetCfgID(2010000)
          root:AddEffect(effect)
          if upper then
            upper:AddEffect(root)
          end
          return composed
        end
      end
    end
  end
end
, [202] = function(args)
  -- function num : 0_195 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2020000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "switchState", 1, "", 0)
      effect:SetCfgID(2020000)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(2020000)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
          effect:SetCfgID(2020000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
            effect:SetCfgID(2020000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2020000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [203] = function(args)
  -- function num : 0_196 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2030000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "switchState", 1, "", 0)
      effect:SetCfgID(2030000)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(2030000)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
          effect:SetCfgID(2030000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
            effect:SetCfgID(2030000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2030000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [204] = function(args)
  -- function num : 0_197 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(15, 15, 3, 1, false)
  effect:SetCfgID(2040000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(15, 15, 3, 1, false)
      effect:SetCfgID(204000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateDestroyEffect)("[switch 120 9]")
        effect:SetCfgID(2040000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateDestroyEffect)("[sceneobject 21]")
            effect:SetCfgID(2040000)
            effect:SetParam("runoninit")
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [205] = function(args)
  -- function num : 0_198 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(2, 26, 3, 1, false)
  effect:SetCfgID(2050000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(2, 26, 3, 1, false)
      effect:SetCfgID(205000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateDestroyEffect)("[switch 111 9]")
        effect:SetCfgID(2050000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateDestroyEffect)("[sceneobject 22]")
            effect:SetCfgID(2050000)
            effect:SetParam("runoninit")
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [206] = function(args)
  -- function num : 0_199 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(4, 31, 3, 1, false)
  effect:SetCfgID(2060000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(4, 31, 3, 1, false)
      effect:SetCfgID(206000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateDestroyEffect)("[switch 121 9]")
        effect:SetCfgID(2060000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateDestroyEffect)("[sceneobject 23]")
            effect:SetCfgID(2060000)
            effect:SetParam("runoninit")
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [207] = function(args)
  -- function num : 0_200 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2070000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateTimelineEffect)(1172, false, false)
      effect:SetCfgID(2070000)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateThawEffect)()
        effect:SetCfgID(2070000)
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateJumpEffect)(10021, 1)
            effect:SetCfgID(2070000)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [208] = function(args)
  -- function num : 0_201 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2080000)
    root:AddEffect(effect)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateThawEffect)()
        effect:SetCfgID(2080000)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [209] = function(args)
  -- function num : 0_202 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(2, 26, 3, 1, true)
  effect:SetCfgID(2090000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(2, 26, 3, 1, true)
      effect:SetCfgID(209000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
      effect:SetCfgID(2090000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(209000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(2090000)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
              effect:SetCfgID(209000000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
              effect:SetCfgID(2090000)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
                  effect:SetCfgID(209000000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                  effect:SetCfgID(2090000)
                  effect:SetParam("runoninit")
                  local upper = effect
                  do
                    do
                      do
                        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                        effect:SetCfgID(209000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [210] = function(args)
  -- function num : 0_203 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(4, 31, 3, 1, true)
  effect:SetCfgID(2100000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(4, 31, 3, 1, true)
      effect:SetCfgID(210000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
      effect:SetCfgID(2100000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(210000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(2100000)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
              effect:SetCfgID(210000000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
              effect:SetCfgID(2100000)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
                  effect:SetCfgID(210000000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                  effect:SetCfgID(2100000)
                  effect:SetParam("runoninit")
                  local upper = effect
                  do
                    do
                      do
                        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                        effect:SetCfgID(210000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [211] = function(args)
  -- function num : 0_204 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2110000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 122 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
    effect:SetCfgID(2110000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 122 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(211000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", false)
        effect:SetCfgID(2110000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", false)
            effect:SetCfgID(211000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2110000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [212] = function(args)
  -- function num : 0_205 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2120000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 122 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(2120000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 122 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(212000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", true)
        effect:SetCfgID(2120000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", true)
            effect:SetCfgID(212000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2120000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [213] = function(args)
  -- function num : 0_206 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2130000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2130000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(213000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(2130000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
            effect:SetCfgID(213000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
            effect:SetCfgID(2130000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
                effect:SetCfgID(213000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (17)", false)
                effect:SetCfgID(2130000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (17)", false)
                    effect:SetCfgID(213000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
                    effect:SetCfgID(2130000)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
                        effect:SetCfgID(213000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
                        effect:SetCfgID(2130000)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
                            effect:SetCfgID(213000000)
                            upper:SetParam("alternative", effect)
                            root:AddEffect(effect)
                            local upper = effect
                            do
                              do
                                local effect = (EffectFactory.CreateThawEffect)()
                                effect:SetCfgID(2130000)
                                root:AddEffect(effect)
                                if upper then
                                  upper:AddEffect(root)
                                end
                                return composed
                              end
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [214] = function(args)
  -- function num : 0_207 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2140000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(2140000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(214000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(2140000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
            effect:SetCfgID(214000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (17)", false)
            effect:SetCfgID(2140000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (17)", false)
                effect:SetCfgID(214000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
                effect:SetCfgID(2140000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
                    effect:SetCfgID(214000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
                    effect:SetCfgID(2140000)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
                        effect:SetCfgID(214000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)()
                            effect:SetCfgID(2140000)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [215] = function(args)
  -- function num : 0_208 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2150000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 122 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2150000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 122 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(215000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(2150000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
            effect:SetCfgID(215000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
            effect:SetCfgID(2150000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
                effect:SetCfgID(215000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
                effect:SetCfgID(2150000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
                    effect:SetCfgID(215000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(2150000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [216] = function(args)
  -- function num : 0_209 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2160000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 122 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2160000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 122 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(216000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
        effect:SetCfgID(2160000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
            effect:SetCfgID(216000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
            effect:SetCfgID(2160000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
                effect:SetCfgID(216000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
                effect:SetCfgID(2160000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
                    effect:SetCfgID(216000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(2160000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [217] = function(args)
  -- function num : 0_210 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2170000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 122 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(2170000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 122 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(217000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", true)
        effect:SetCfgID(2170000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", true)
            effect:SetCfgID(217000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2170000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [218] = function(args)
  -- function num : 0_211 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2180000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 135 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
    effect:SetCfgID(2180000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 135 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(218000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(2180000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
            effect:SetCfgID(218000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2180000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [219] = function(args)
  -- function num : 0_212 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2190000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 135 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(2190000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 135 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(219000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(2190000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
            effect:SetCfgID(219000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2190000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [220] = function(args)
  -- function num : 0_213 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2200000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 135 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(2200000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 135 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(220000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(2200000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
            effect:SetCfgID(220000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2200000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [221] = function(args)
  -- function num : 0_214 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2210000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 135 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2210000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 135 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(221000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
        effect:SetCfgID(2210000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
            effect:SetCfgID(221000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (25)", false)
            effect:SetCfgID(2210000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (25)", false)
                effect:SetCfgID(221000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (26)", false)
                effect:SetCfgID(2210000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (26)", false)
                    effect:SetCfgID(221000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(2210000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [222] = function(args)
  -- function num : 0_215 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2220000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 135 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2220000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 135 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(222000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(2220000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
            effect:SetCfgID(222000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
            effect:SetCfgID(2220000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
                effect:SetCfgID(222000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (25)", false)
                effect:SetCfgID(2220000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (25)", false)
                    effect:SetCfgID(222000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (26)", false)
                    effect:SetCfgID(2220000)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (26)", false)
                        effect:SetCfgID(222000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)()
                            effect:SetCfgID(2220000)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [223] = function(args)
  -- function num : 0_216 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2230000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2230000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(223000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
        effect:SetCfgID(2230000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
            effect:SetCfgID(223000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
            effect:SetCfgID(2230000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
                effect:SetCfgID(223000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (18)", false)
                effect:SetCfgID(2230000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (18)", false)
                    effect:SetCfgID(223000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(2230000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [224] = function(args)
  -- function num : 0_217 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2240000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(2240000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(224000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", true)
        effect:SetCfgID(2240000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", true)
            effect:SetCfgID(224000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (18)", false)
            effect:SetCfgID(2240000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (18)", false)
                effect:SetCfgID(224000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(2240000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [225] = function(args)
  -- function num : 0_218 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2250000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 136 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
    effect:SetCfgID(2250000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 136 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(225000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
        effect:SetCfgID(2250000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
            effect:SetCfgID(225000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2250000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [226] = function(args)
  -- function num : 0_219 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2260000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 136 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(2260000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 136 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(226000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (6)", true)
        effect:SetCfgID(2260000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (6)", true)
            effect:SetCfgID(226000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2260000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [227] = function(args)
  -- function num : 0_220 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2270000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 136 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(2270000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 136 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(227000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (6)", true)
        effect:SetCfgID(2270000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (6)", true)
            effect:SetCfgID(227000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2270000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [228] = function(args)
  -- function num : 0_221 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2280000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 136 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2280000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 136 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(228000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
        effect:SetCfgID(2280000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
            effect:SetCfgID(228000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (16)", false)
            effect:SetCfgID(2280000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (16)", false)
                effect:SetCfgID(228000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(2280000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [229] = function(args)
  -- function num : 0_222 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2290000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 136 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2290000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 136 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(229000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
        effect:SetCfgID(2290000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
            effect:SetCfgID(229000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (16)", false)
            effect:SetCfgID(2290000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (16)", false)
                effect:SetCfgID(229000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(2290000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [230] = function(args)
  -- function num : 0_223 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2300000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 136 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(2300000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 136 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(230000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
        effect:SetCfgID(2300000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
            effect:SetCfgID(230000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (16)", true)
            effect:SetCfgID(2300000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (16)", true)
                effect:SetCfgID(230000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(2300000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [231] = function(args)
  -- function num : 0_224 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "", 0)
  effect:SetCfgID(2310000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
      effect:SetCfgID(231000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(16, 6, 1, 1, true)
      effect:SetCfgID(2310000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(16, 6, 1, 1, true)
            effect:SetCfgID(231000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [232] = function(args)
  -- function num : 0_225 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "", 0)
  effect:SetCfgID(2320000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
      effect:SetCfgID(232000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(16, 6, 1, 1, false)
      effect:SetCfgID(2320000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(16, 6, 1, 1, false)
            effect:SetCfgID(232000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [233] = function(args)
  -- function num : 0_226 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2330000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2330000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(233000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM09")
          effect:SetCfgID(2330000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(2330000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(2330000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(2330000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [234] = function(args)
  -- function num : 0_227 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2340000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2340000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(234000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM09")
          effect:SetCfgID(2340000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(2340000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(2340000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(2340000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [235] = function(args)
  -- function num : 0_228 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2350000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 139 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
    effect:SetCfgID(2350000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 139 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(235000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", false)
        effect:SetCfgID(2350000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", false)
            effect:SetCfgID(235000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2350000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [236] = function(args)
  -- function num : 0_229 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2360000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 139 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(2360000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 139 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(236000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
        effect:SetCfgID(2360000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
            effect:SetCfgID(236000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2360000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [237] = function(args)
  -- function num : 0_230 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2370000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 139 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(2370000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 139 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(237000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
        effect:SetCfgID(2370000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
            effect:SetCfgID(237000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2370000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [238] = function(args)
  -- function num : 0_231 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2380000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 139 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2380000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 139 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(238000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
        effect:SetCfgID(2380000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
            effect:SetCfgID(238000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2380000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [239] = function(args)
  -- function num : 0_232 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2390000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 139 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2390000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 139 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(239000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
        effect:SetCfgID(2390000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
            effect:SetCfgID(239000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2390000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [240] = function(args)
  -- function num : 0_233 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2400000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 139 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2400000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 139 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(240000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
        effect:SetCfgID(2400000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
            effect:SetCfgID(240000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
            effect:SetCfgID(2400000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
                effect:SetCfgID(240000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(2400000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [241] = function(args)
  -- function num : 0_234 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2410000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 139 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(2410000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 139 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(241000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
        effect:SetCfgID(2410000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
            effect:SetCfgID(241000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (11)", true)
            effect:SetCfgID(2410000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (11)", true)
                effect:SetCfgID(241000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(2410000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [242] = function(args)
  -- function num : 0_235 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2420000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 139 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2420000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 139 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(242000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (12)", true)
        effect:SetCfgID(2420000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (12)", true)
            effect:SetCfgID(242000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
            effect:SetCfgID(2420000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
                effect:SetCfgID(242000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(2420000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [243] = function(args)
  -- function num : 0_236 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "", 0)
  effect:SetCfgID(2430000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
      effect:SetCfgID(243000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(26, 20, 1, 1, true)
      effect:SetCfgID(2430000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(26, 20, 1, 1, true)
            effect:SetCfgID(243000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [244] = function(args)
  -- function num : 0_237 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "", 0)
  effect:SetCfgID(2440000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
      effect:SetCfgID(244000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(26, 20, 1, 1, false)
      effect:SetCfgID(2440000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(26, 20, 1, 1, false)
            effect:SetCfgID(244000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [245] = function(args)
  -- function num : 0_238 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2450000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
    effect:SetCfgID(2450000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(245000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", false)
        effect:SetCfgID(2450000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", false)
            effect:SetCfgID(245000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2450000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [246] = function(args)
  -- function num : 0_239 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2460000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(2460000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(246000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
        effect:SetCfgID(2460000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
            effect:SetCfgID(246000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2460000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [247] = function(args)
  -- function num : 0_240 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2470000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(2470000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(247000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
        effect:SetCfgID(2470000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
            effect:SetCfgID(247000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2470000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [248] = function(args)
  -- function num : 0_241 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2480000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2480000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(248000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
        effect:SetCfgID(2480000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
            effect:SetCfgID(248000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
            effect:SetCfgID(2480000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
                effect:SetCfgID(248000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (19)", false)
                effect:SetCfgID(2480000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (19)", false)
                    effect:SetCfgID(248000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
                    effect:SetCfgID(2480000)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
                        effect:SetCfgID(248000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)()
                            effect:SetCfgID(2480000)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [249] = function(args)
  -- function num : 0_242 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2490000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2490000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(249000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
        effect:SetCfgID(2490000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
            effect:SetCfgID(249000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (19)", false)
            effect:SetCfgID(2490000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (19)", false)
                effect:SetCfgID(249000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
                effect:SetCfgID(2490000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
                    effect:SetCfgID(249000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(2490000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [250] = function(args)
  -- function num : 0_243 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2500000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2500000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(250000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
        effect:SetCfgID(2500000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
            effect:SetCfgID(250000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
            effect:SetCfgID(2500000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
                effect:SetCfgID(250000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(2500000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [251] = function(args)
  -- function num : 0_244 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2510000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(2510000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(251000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
        effect:SetCfgID(2510000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
            effect:SetCfgID(251000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (11)", true)
            effect:SetCfgID(2510000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (11)", true)
                effect:SetCfgID(251000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
                effect:SetCfgID(2510000)
                effect:SetParam("runoninit")
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
                    effect:SetCfgID(251000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (27)", true)
                    effect:SetCfgID(2510000)
                    effect:SetParam("runoninit")
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (27)", true)
                        effect:SetCfgID(251000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)()
                            effect:SetCfgID(2510000)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [252] = function(args)
  -- function num : 0_245 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2520000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2520000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(252000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (12)", true)
        effect:SetCfgID(2520000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (12)", true)
            effect:SetCfgID(252000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
            effect:SetCfgID(2520000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
                effect:SetCfgID(252000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
                effect:SetCfgID(2520000)
                effect:SetParam("runoninit")
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
                    effect:SetCfgID(252000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(2520000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [253] = function(args)
  -- function num : 0_246 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2530000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2530000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(253000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (13)", false)
        effect:SetCfgID(2530000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (13)", false)
            effect:SetCfgID(253000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
            effect:SetCfgID(2530000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
                effect:SetCfgID(253000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (20)", false)
                effect:SetCfgID(2530000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (20)", false)
                    effect:SetCfgID(253000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
                    effect:SetCfgID(2530000)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
                        effect:SetCfgID(253000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)()
                            effect:SetCfgID(2530000)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [254] = function(args)
  -- function num : 0_247 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2540000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(2540000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(254000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (13)", true)
        effect:SetCfgID(2540000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (13)", true)
            effect:SetCfgID(254000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (20)", false)
            effect:SetCfgID(2540000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (20)", false)
                effect:SetCfgID(254000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (28)", true)
                effect:SetCfgID(2540000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (28)", true)
                    effect:SetCfgID(254000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(2540000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [255] = function(args)
  -- function num : 0_248 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2550000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2550000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(255000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (14)", false)
        effect:SetCfgID(2550000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (14)", false)
            effect:SetCfgID(255000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
            effect:SetCfgID(2550000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
                effect:SetCfgID(255000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (21)", false)
                effect:SetCfgID(2550000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (21)", false)
                    effect:SetCfgID(255000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
                    effect:SetCfgID(2550000)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
                        effect:SetCfgID(255000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)()
                            effect:SetCfgID(2550000)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [256] = function(args)
  -- function num : 0_249 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2560000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(2560000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(256000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (13)", false)
        effect:SetCfgID(2560000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (13)", false)
            effect:SetCfgID(256000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (14)", true)
            effect:SetCfgID(2560000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (14)", true)
                effect:SetCfgID(256000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (21)", false)
                effect:SetCfgID(2560000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (21)", false)
                    effect:SetCfgID(256000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (28)", true)
                    effect:SetCfgID(2560000)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (28)", true)
                        effect:SetCfgID(256000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)()
                            effect:SetCfgID(2560000)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [257] = function(args)
  -- function num : 0_250 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2570000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2570000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(257000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (15)", false)
        effect:SetCfgID(2570000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (15)", false)
            effect:SetCfgID(257000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
            effect:SetCfgID(2570000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
                effect:SetCfgID(257000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (22)", false)
                effect:SetCfgID(2570000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (22)", false)
                    effect:SetCfgID(257000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(2570000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [258] = function(args)
  -- function num : 0_251 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2580000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2580000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(258000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (15)", true)
        effect:SetCfgID(2580000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (15)", true)
            effect:SetCfgID(258000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (22)", false)
            effect:SetCfgID(2580000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (22)", false)
                effect:SetCfgID(258000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(2580000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [259] = function(args)
  -- function num : 0_252 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2590000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2590000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(259000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(2590000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(2590000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(2590000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(2590000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [260] = function(args)
  -- function num : 0_253 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2600000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2600000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(260000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(2600000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(2600000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(2600000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(2600000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [261] = function(args)
  -- function num : 0_254 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2610000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2610000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(261000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
          effect:SetCfgID(2610000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 142 2]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
                effect:SetCfgID(2610000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 142 3]")
                    root:Add("[trap 142 4]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(2610000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(2610000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [262] = function(args)
  -- function num : 0_255 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2620000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2620000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(262000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
          effect:SetCfgID(2620000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 142 2]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
                effect:SetCfgID(2620000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 142 3]")
                    root:Add("[trap 142 4]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(2620000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(2620000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [263] = function(args)
  -- function num : 0_256 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2630000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2630000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(263000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(2630000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 143 2]")
              root:Add("[trap 143 3]")
              root:Add("[trap 143 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                effect:SetCfgID(2630000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 143 5]")
                    root:Add("[trap 143 6]")
                    root:Add("[trap 143 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(2630000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(2630000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [264] = function(args)
  -- function num : 0_257 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2640000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2640000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(264000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(2640000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 143 2]")
              root:Add("[trap 143 3]")
              root:Add("[trap 143 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                effect:SetCfgID(2640000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 143 5]")
                    root:Add("[trap 143 6]")
                    root:Add("[trap 143 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(2640000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(2640000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [265] = function(args)
  -- function num : 0_258 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2650000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2650000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(265000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(2650000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 144 2]")
              root:Add("[trap 144 3]")
              root:Add("[trap 144 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                effect:SetCfgID(2650000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 144 5]")
                    root:Add("[trap 144 6]")
                    root:Add("[trap 144 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(2650000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(2650000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [266] = function(args)
  -- function num : 0_259 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2660000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2660000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(266000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(2660000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 144 2]")
              root:Add("[trap 144 3]")
              root:Add("[trap 144 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                effect:SetCfgID(2660000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 144 5]")
                    root:Add("[trap 144 6]")
                    root:Add("[trap 144 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(2660000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(2660000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [267] = function(args)
  -- function num : 0_260 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2670000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2670000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(267000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(2670000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(2670000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(2670000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(2670000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [268] = function(args)
  -- function num : 0_261 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2680000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2680000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(268000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(2680000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(2680000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(2680000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(2680000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [269] = function(args)
  -- function num : 0_262 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2690000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2690000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(269000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(2690000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(2690000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(2690000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(2690000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [270] = function(args)
  -- function num : 0_263 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2700000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2700000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(270000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(2700000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(2700000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(2700000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(2700000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [271] = function(args)
  -- function num : 0_264 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2710000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2710000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(271000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(2710000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(2710000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(2710000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(2710000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [272] = function(args)
  -- function num : 0_265 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2720000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2720000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(272000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(2720000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(2720000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(2720000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(2720000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [273] = function(args)
  -- function num : 0_266 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2730000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateEventEffect)(100402)
      effect:SetCfgID(2730000)
      root:AddEffect(effect)
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateThawEffect)()
          effect:SetCfgID(2730000)
          root:AddEffect(effect)
          if upper then
            upper:AddEffect(root)
          end
          return composed
        end
      end
    end
  end
end
, [274] = function(args)
  -- function num : 0_267 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = root
  local root = (ChangeTrapStateEffect.Create)()
  local upper = root
  do
    do
      do
        local root = ((ChangeTrapStateEffect.Sequence).Create)()
        root:Add("[switch 148 1]")
        upper:Add(root)
        upper:AddEffect(root)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [275] = function(args)
  -- function num : 0_268 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = root
  local root = (ChangeTrapStateEffect.Create)()
  local upper = root
  do
    do
      do
        local root = ((ChangeTrapStateEffect.Sequence).Create)()
        root:Add("[switch 148 2]")
        upper:Add(root)
        upper:AddEffect(root)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [276] = function(args)
  -- function num : 0_269 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = root
  local root = (ChangeTrapStateEffect.Create)()
  local upper = root
  do
    do
      do
        local root = ((ChangeTrapStateEffect.Sequence).Create)()
        root:Add("[switch 148 3]")
        upper:Add(root)
        upper:AddEffect(root)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [277] = function(args)
  -- function num : 0_270 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = root
  local root = (ChangeTrapStateEffect.Create)()
  local upper = root
  do
    do
      do
        local root = ((ChangeTrapStateEffect.Sequence).Create)()
        root:Add("[switch 148 4]")
        upper:Add(root)
        upper:AddEffect(root)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [278] = function(args)
  -- function num : 0_271 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(15, 15, 3, 1, true)
  effect:SetCfgID(2780000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateBlockChangeEffect)(15, 15, 3, 1, true)
        effect:SetCfgID(278000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [279] = function(args)
  -- function num : 0_272 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(2, 26, 3, 1, true)
  effect:SetCfgID(2790000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateBlockChangeEffect)(2, 26, 3, 1, true)
        effect:SetCfgID(279000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [280] = function(args)
  -- function num : 0_273 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2800000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
      effect:SetCfgID(2800000)
      root:AddEffect(effect)
      local upper = root or composed
      local root = nil
      if not composed then
        composed = (EffectFactory.CreateEffectList)()
        root = composed
      else
        root = (EffectFactory.CreateEffectList)()
      end
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(280000000)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(280000000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
            effect:SetCfgID(280000000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                effect:SetCfgID(280000000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                local upper = effect
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trigger", 1, "TowerIdle", 1)
                  effect:SetCfgID(2800000)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                    effect:SetCfgID(2800000)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(2800000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [281] = function(args)
  -- function num : 0_274 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2810000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
      effect:SetCfgID(2810000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
      effect:SetCfgID(2810000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(281000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
            effect:SetCfgID(2810000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2810000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [282] = function(args)
  -- function num : 0_275 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2820000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
      effect:SetCfgID(2820000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
      effect:SetCfgID(2820000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(282000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
            effect:SetCfgID(2820000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2820000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [283] = function(args)
  -- function num : 0_276 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2830000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
      effect:SetCfgID(2830000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
      effect:SetCfgID(2830000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(283000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
            effect:SetCfgID(2830000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2830000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [284] = function(args)
  -- function num : 0_277 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2840000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
      effect:SetCfgID(2840000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 1, "TowerOn4", 1)
      effect:SetCfgID(2840000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 1, "", 1)
          effect:SetCfgID(284000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
            effect:SetCfgID(2840000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2840000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [285] = function(args)
  -- function num : 0_278 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2850000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
      effect:SetCfgID(2850000)
      root:AddEffect(effect)
      local upper = root or composed
      local root = nil
      if not composed then
        composed = (EffectFactory.CreateEffectList)()
        root = composed
      else
        root = (EffectFactory.CreateEffectList)()
      end
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(285000000)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(285000000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
            effect:SetCfgID(285000000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                effect:SetCfgID(285000000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                local upper = effect
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trigger", 1, "TowerIdle", 1)
                  effect:SetCfgID(2850000)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                    effect:SetCfgID(2850000)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(2850000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [286] = function(args)
  -- function num : 0_279 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2860000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
      effect:SetCfgID(2860000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
      effect:SetCfgID(2860000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(286000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
            effect:SetCfgID(2860000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2860000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [287] = function(args)
  -- function num : 0_280 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2870000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
      effect:SetCfgID(2870000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
      effect:SetCfgID(2870000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(287000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
            effect:SetCfgID(2870000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2870000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [288] = function(args)
  -- function num : 0_281 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2880000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
      effect:SetCfgID(2880000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
      effect:SetCfgID(2880000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(288000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
            effect:SetCfgID(2880000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2880000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [289] = function(args)
  -- function num : 0_282 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2890000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
      effect:SetCfgID(2890000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 1, "TowerOn4", 1)
      effect:SetCfgID(2890000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 1, "", 1)
          effect:SetCfgID(289000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
            effect:SetCfgID(2890000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(2890000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [290] = function(args)
  -- function num : 0_283 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(4, 31, 3, 1, true)
  effect:SetCfgID(2900000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateBlockChangeEffect)(4, 31, 3, 1, true)
        effect:SetCfgID(290000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [291] = function(args)
  -- function num : 0_284 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2910000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2910000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(291000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(2910000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(2910000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(2910000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(2910000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [292] = function(args)
  -- function num : 0_285 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2920000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2920000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(292000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(2920000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(2920000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(2920000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(2920000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [293] = function(args)
  -- function num : 0_286 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2930000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2930000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(293000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(2930000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(2930000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(2930000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(2930000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [294] = function(args)
  -- function num : 0_287 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2940000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2940000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(294000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(2940000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(2940000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(2940000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(2940000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [295] = function(args)
  -- function num : 0_288 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
      effect:SetCfgID(2950000)
      root:AddEffect(effect)
      if upper then
        upper:AddEffect(root)
      end
      return composed
    end
  end
end
, [296] = function(args)
  -- function num : 0_289 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
  effect:SetCfgID(2960000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(296000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [297] = function(args)
  -- function num : 0_290 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
  effect:SetCfgID(2970000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(297000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [298] = function(args)
  -- function num : 0_291 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
      effect:SetCfgID(2980000)
      root:AddEffect(effect)
      if upper then
        upper:AddEffect(root)
      end
      return composed
    end
  end
end
, [299] = function(args)
  -- function num : 0_292 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2990000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2990000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(299000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(2990000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 153 2]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(2990000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 153 3]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(2990000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(2990000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [300] = function(args)
  -- function num : 0_293 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3000000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3000000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(300000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3000000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 153 2]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(3000000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 153 3]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(3000000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(3000000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [301] = function(args)
  -- function num : 0_294 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3010000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3010000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(301000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3010000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3010000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3010000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3010000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [302] = function(args)
  -- function num : 0_295 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3020000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3020000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(302000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3020000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3020000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3020000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3020000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [303] = function(args)
  -- function num : 0_296 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3030000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3030000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(303000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3030000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 155 2]")
              root:Add("[trap 155 3]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
                effect:SetCfgID(3030000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 155 4]")
                    root:Add("[trap 155 5]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(3030000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(3030000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [304] = function(args)
  -- function num : 0_297 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3040000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3040000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(304000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3040000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 155 2]")
              root:Add("[trap 155 3]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
                effect:SetCfgID(3040000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 155 4]")
                    root:Add("[trap 155 5]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(3040000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(3040000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [305] = function(args)
  -- function num : 0_298 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3050000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3050000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(305000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3050000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 156 2]")
              root:Add("[trap 156 3]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(3050000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 156 4]")
                    root:Add("[trap 156 5]")
                    root:Add("[trap 156 6]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(3050000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(3050000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [306] = function(args)
  -- function num : 0_299 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3060000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3060000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(306000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3060000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 156 2]")
              root:Add("[trap 156 3]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(3060000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 156 4]")
                    root:Add("[trap 156 5]")
                    root:Add("[trap 156 6]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(3060000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(3060000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [307] = function(args)
  -- function num : 0_300 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3070000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 157 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
    effect:SetCfgID(3070000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 157 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(307000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", false)
        effect:SetCfgID(3070000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", false)
            effect:SetCfgID(307000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(3070000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [308] = function(args)
  -- function num : 0_301 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3080000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 157 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(3080000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 157 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(308000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", true)
        effect:SetCfgID(3080000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", true)
            effect:SetCfgID(308000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(3080000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [309] = function(args)
  -- function num : 0_302 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3090000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 157 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(3090000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 157 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(309000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", true)
        effect:SetCfgID(3090000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", true)
            effect:SetCfgID(309000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(3090000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [310] = function(args)
  -- function num : 0_303 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "DoorClose", 0.1)
  effect:SetCfgID(3100000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
      effect:SetCfgID(310000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(13, 8, 1, 1, true)
      effect:SetCfgID(3100000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(13, 8, 1, 1, true)
            effect:SetCfgID(310000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [311] = function(args)
  -- function num : 0_304 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "DoorOpen", 0.1)
  effect:SetCfgID(311031100)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
      effect:SetCfgID(31103110000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(13, 8, 1, 1, false)
      effect:SetCfgID(311031100)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(13, 8, 1, 1, false)
            effect:SetCfgID(31103110000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [312] = function(args)
  -- function num : 0_305 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3120000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(3120000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(312000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(3120000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
            effect:SetCfgID(312000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
            effect:SetCfgID(3120000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
                effect:SetCfgID(312000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(3120000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [313] = function(args)
  -- function num : 0_306 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3130000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(3130000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(313000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(3130000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
            effect:SetCfgID(313000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
            effect:SetCfgID(3130000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
                effect:SetCfgID(313000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
                effect:SetCfgID(3130000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
                    effect:SetCfgID(313000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(3130000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [314] = function(args)
  -- function num : 0_307 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3140000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 157 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(3140000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 157 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(314000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(3140000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
            effect:SetCfgID(314000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
            effect:SetCfgID(3140000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
                effect:SetCfgID(314000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
                effect:SetCfgID(3140000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
                    effect:SetCfgID(314000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
                    effect:SetCfgID(3140000)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
                        effect:SetCfgID(314000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", false)
                        effect:SetCfgID(3140000)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
                            effect:SetCfgID(314000000)
                            upper:SetParam("alternative", effect)
                            root:AddEffect(effect)
                            local upper = effect
                            do
                              do
                                local effect = (EffectFactory.CreateThawEffect)()
                                effect:SetCfgID(3140000)
                                root:AddEffect(effect)
                                if upper then
                                  upper:AddEffect(root)
                                end
                                return composed
                              end
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [315] = function(args)
  -- function num : 0_308 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3150000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 157 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(3150000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 157 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(315000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(3150000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
            effect:SetCfgID(315000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
            effect:SetCfgID(3150000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
                effect:SetCfgID(315000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", false)
                effect:SetCfgID(3150000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
                    effect:SetCfgID(315000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(3150000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [316] = function(args)
  -- function num : 0_309 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3160000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(3160000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(316000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(3160000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
            effect:SetCfgID(316000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
            effect:SetCfgID(3160000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
                effect:SetCfgID(316000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
                effect:SetCfgID(3160000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
                    effect:SetCfgID(316000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(3160000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [317] = function(args)
  -- function num : 0_310 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3170000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(3170000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(317000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(3170000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
            effect:SetCfgID(317000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(3170000)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateVisualEffect)("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
              effect:SetCfgID(3170000)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateVisualEffect)("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
                  effect:SetCfgID(317000000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
                  effect:SetCfgID(3170000)
                  local upper = effect
                  do
                    do
                      local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
                      effect:SetCfgID(317000000)
                      upper:SetParam("alternative", effect)
                      root:AddEffect(effect)
                      local upper = effect
                      local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
                      effect:SetCfgID(3170000)
                      local upper = effect
                      do
                        do
                          do
                            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
                            effect:SetCfgID(317000000)
                            upper:SetParam("alternative", effect)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [318] = function(args)
  -- function num : 0_311 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3180000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 157 5]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(3180000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 157 5]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(318000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(3180000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
            effect:SetCfgID(318000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
            effect:SetCfgID(3180000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
                effect:SetCfgID(318000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
                effect:SetCfgID(3180000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
                    effect:SetCfgID(318000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (10)", false)
                    effect:SetCfgID(3180000)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (10)", false)
                        effect:SetCfgID(318000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)()
                            effect:SetCfgID(3180000)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [319] = function(args)
  -- function num : 0_312 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3190000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 157 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(3190000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 157 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(319000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(3190000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
            effect:SetCfgID(319000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
            effect:SetCfgID(3190000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
                effect:SetCfgID(319000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (10)", false)
                effect:SetCfgID(3190000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (10)", false)
                    effect:SetCfgID(319000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(3190000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [320] = function(args)
  -- function num : 0_313 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3200000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3200000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(320000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3200000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3200000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3200000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3200000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [321] = function(args)
  -- function num : 0_314 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3210000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3210000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(321000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3210000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3210000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3210000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3210000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [322] = function(args)
  -- function num : 0_315 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3220000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3220000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(322000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3220000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3220000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3220000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3220000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [323] = function(args)
  -- function num : 0_316 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3230000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3230000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(323000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3230000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3230000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3230000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3230000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [324] = function(args)
  -- function num : 0_317 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3240000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3240000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(324000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3240000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3240000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3240000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3240000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [325] = function(args)
  -- function num : 0_318 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3250000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3250000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(325000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3250000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3250000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3250000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3250000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [326] = function(args)
  -- function num : 0_319 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3260000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3260000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(326000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3260000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3260000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3260000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3260000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [327] = function(args)
  -- function num : 0_320 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3270000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3270000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(327000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3270000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3270000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3270000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3270000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [328] = function(args)
  -- function num : 0_321 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3280000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3280000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(328000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3280000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3280000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3280000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3280000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [329] = function(args)
  -- function num : 0_322 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3290000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3290000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(329000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3290000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3290000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3290000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3290000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [330] = function(args)
  -- function num : 0_323 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3300000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3300000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(330000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(3300000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3300000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3300000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3300000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [331] = function(args)
  -- function num : 0_324 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3310000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3310000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(331000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(3310000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3310000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3310000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3310000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [332] = function(args)
  -- function num : 0_325 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3320000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3320000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(332000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(3320000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3320000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3320000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3320000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [333] = function(args)
  -- function num : 0_326 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3330000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3330000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(333000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(3330000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3330000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3330000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3330000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [334] = function(args)
  -- function num : 0_327 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3340000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3340000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(334000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3340000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 162 3]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
                effect:SetCfgID(3340000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 162 6]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(3340000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(3340000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [335] = function(args)
  -- function num : 0_328 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3350000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3350000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(335000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3350000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 162 3]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
                effect:SetCfgID(3350000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 162 6]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(3350000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(3350000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [336] = function(args)
  -- function num : 0_329 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3360000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3360000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(336000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3360000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 162 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                effect:SetCfgID(3360000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 162 5]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
                      effect:SetCfgID(3360000)
                      root:AddEffect(effect)
                      local upper = root
                      local root = (ChangeTrapStateEffect.Create)()
                      local upper = root
                      do
                        do
                          local root = ((ChangeTrapStateEffect.Parallel).Create)()
                          root:Add("[trap 162 6]")
                          upper:Add(root)
                          upper:AddEffect(root)
                          local upper = effect
                          do
                            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                            effect:SetCfgID(3360000)
                            root:AddEffect(effect)
                            local upper = effect
                            do
                              do
                                local effect = (EffectFactory.CreateThawEffect)()
                                effect:SetCfgID(3360000)
                                root:AddEffect(effect)
                                if upper then
                                  upper:AddEffect(root)
                                end
                                return composed
                              end
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [337] = function(args)
  -- function num : 0_330 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3370000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3370000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(337000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3370000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 162 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                effect:SetCfgID(3370000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 162 5]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
                      effect:SetCfgID(3370000)
                      root:AddEffect(effect)
                      local upper = root
                      local root = (ChangeTrapStateEffect.Create)()
                      local upper = root
                      do
                        do
                          local root = ((ChangeTrapStateEffect.Parallel).Create)()
                          root:Add("[trap 162 6]")
                          upper:Add(root)
                          upper:AddEffect(root)
                          local upper = effect
                          do
                            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                            effect:SetCfgID(3370000)
                            root:AddEffect(effect)
                            local upper = effect
                            do
                              do
                                local effect = (EffectFactory.CreateThawEffect)()
                                effect:SetCfgID(3370000)
                                root:AddEffect(effect)
                                if upper then
                                  upper:AddEffect(root)
                                end
                                return composed
                              end
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [338] = function(args)
  -- function num : 0_331 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3380000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 163 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
    effect:SetCfgID(3380000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 163 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(338000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", false)
        effect:SetCfgID(3380000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", false)
            effect:SetCfgID(338000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(3380000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [339] = function(args)
  -- function num : 0_332 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3390000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 163 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(3390000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 163 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(339000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", true)
        effect:SetCfgID(3390000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang", true)
            effect:SetCfgID(339000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(3390000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [340] = function(args)
  -- function num : 0_333 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3400000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(3400000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(340000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(3400000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
            effect:SetCfgID(340000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
            effect:SetCfgID(3400000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
                effect:SetCfgID(340000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
                effect:SetCfgID(3400000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
                    effect:SetCfgID(340000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(3400000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [341] = function(args)
  -- function num : 0_334 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3410000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(3410000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(341000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(3410000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
            effect:SetCfgID(341000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
            effect:SetCfgID(3410000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
                effect:SetCfgID(341000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(3410000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [342] = function(args)
  -- function num : 0_335 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3420000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 163 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(3420000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 163 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(342000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(3420000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
            effect:SetCfgID(342000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
            effect:SetCfgID(3420000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
                effect:SetCfgID(342000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
                effect:SetCfgID(3420000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
                    effect:SetCfgID(342000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(3420000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [343] = function(args)
  -- function num : 0_336 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3430000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 163 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(3430000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 163 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(343000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
        effect:SetCfgID(3430000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
            effect:SetCfgID(343000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
            effect:SetCfgID(3430000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
                effect:SetCfgID(343000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
                effect:SetCfgID(3430000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
                    effect:SetCfgID(343000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(3430000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [344] = function(args)
  -- function num : 0_337 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "DoorClose", 0.1)
  effect:SetCfgID(3440000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
      effect:SetCfgID(344000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(5, 12, 1, 1, true)
      effect:SetCfgID(3440000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(5, 12, 1, 1, true)
            effect:SetCfgID(344000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [345] = function(args)
  -- function num : 0_338 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "DoorOpen", 0.1)
  effect:SetCfgID(345031100)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
      effect:SetCfgID(34503110000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(5, 12, 1, 1, false)
      effect:SetCfgID(345031100)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(5, 12, 1, 1, false)
            effect:SetCfgID(34503110000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [346] = function(args)
  -- function num : 0_339 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3460000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3460000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(346000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(3460000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3460000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3460000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3460000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [347] = function(args)
  -- function num : 0_340 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3470000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3470000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(347000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(3470000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3470000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3470000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3470000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [348] = function(args)
  -- function num : 0_341 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3480000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3480000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(348000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3480000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3480000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3480000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3480000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [349] = function(args)
  -- function num : 0_342 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3490000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3490000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(349000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3490000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3490000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3490000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3490000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [350] = function(args)
  -- function num : 0_343 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3500000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3500000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(350000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3500000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3500000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3500000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3500000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [351] = function(args)
  -- function num : 0_344 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3510000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3510000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(351000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3510000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3510000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3510000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3510000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [352] = function(args)
  -- function num : 0_345 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3520000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3520000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(352000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3520000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3520000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3520000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3520000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [353] = function(args)
  -- function num : 0_346 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3530000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3530000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(353000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3530000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3530000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3530000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3530000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [354] = function(args)
  -- function num : 0_347 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3540000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
      effect:SetCfgID(3540000)
      root:AddEffect(effect)
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateThawEffect)()
          effect:SetCfgID(3540000)
          root:AddEffect(effect)
          if upper then
            upper:AddEffect(root)
          end
          return composed
        end
      end
    end
  end
end
, [355] = function(args)
  -- function num : 0_348 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3550000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
      effect:SetCfgID(3550000)
      root:AddEffect(effect)
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateThawEffect)()
          effect:SetCfgID(3550000)
          root:AddEffect(effect)
          if upper then
            upper:AddEffect(root)
          end
          return composed
        end
      end
    end
  end
end
, [356] = function(args)
  -- function num : 0_349 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3560000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
      effect:SetCfgID(3560000)
      root:AddEffect(effect)
      local upper = root
      local root = (ChangeTrapStateEffect.Create)()
      local upper = root
      do
        do
          local root = ((ChangeTrapStateEffect.Parallel).Create)()
          root:Add("[switch 168 5]")
          upper:Add(root)
          upper:AddEffect(root)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
            effect:SetCfgID(3560000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(3560000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [357] = function(args)
  -- function num : 0_350 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "", 0)
  effect:SetCfgID(3570000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "", 0)
      effect:SetCfgID(357000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(34, 12, 3, 1, true)
      effect:SetCfgID(3570000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(34, 12, 3, 1, true)
            effect:SetCfgID(357000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [358] = function(args)
  -- function num : 0_351 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "", 0)
  effect:SetCfgID(3580000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "", 0)
      effect:SetCfgID(358000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(34, 12, 3, 1, false)
      effect:SetCfgID(3580000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(34, 12, 3, 1, false)
            effect:SetCfgID(358000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [359] = function(args)
  -- function num : 0_352 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3590000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
      effect:SetCfgID(3590000)
      root:AddEffect(effect)
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateThawEffect)()
          effect:SetCfgID(3590000)
          root:AddEffect(effect)
          if upper then
            upper:AddEffect(root)
          end
          return composed
        end
      end
    end
  end
end
, [360] = function(args)
  -- function num : 0_353 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3600000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
      effect:SetCfgID(3600000)
      root:AddEffect(effect)
      local upper = root
      local root = (ChangeTrapStateEffect.Create)()
      local upper = root
      do
        do
          local root = ((ChangeTrapStateEffect.Parallel).Create)()
          root:Add("[switch 171 4]")
          upper:Add(root)
          upper:AddEffect(root)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
            effect:SetCfgID(3600000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(3600000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [361] = function(args)
  -- function num : 0_354 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "", 0)
  effect:SetCfgID(3610000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "", 0)
      effect:SetCfgID(361000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(24, 10, 3, 1, true)
      effect:SetCfgID(3610000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(24, 10, 3, 1, true)
            effect:SetCfgID(361000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [362] = function(args)
  -- function num : 0_355 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "", 0)
  effect:SetCfgID(3620000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "", 0)
      effect:SetCfgID(362000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(24, 10, 3, 1, false)
      effect:SetCfgID(3620000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(24, 10, 3, 1, false)
            effect:SetCfgID(362000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [363] = function(args)
  -- function num : 0_356 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3630000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3630000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(363000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3630000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3630000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3630000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3630000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [364] = function(args)
  -- function num : 0_357 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3640000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3640000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(364000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3640000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3640000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3640000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3640000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [365] = function(args)
  -- function num : 0_358 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3650000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3650000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(365000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3650000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3650000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3650000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3650000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [366] = function(args)
  -- function num : 0_359 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3660000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3660000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(366000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3660000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3660000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3660000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3660000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [367] = function(args)
  -- function num : 0_360 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3670000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3670000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(367000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(3670000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3670000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3670000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3670000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [368] = function(args)
  -- function num : 0_361 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3680000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3680000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(368000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(3680000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3680000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3680000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3680000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [369] = function(args)
  -- function num : 0_362 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3690000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3690000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(369000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3690000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3690000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3690000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3690000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [370] = function(args)
  -- function num : 0_363 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3700000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3700000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(370000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3700000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3700000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3700000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3700000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [371] = function(args)
  -- function num : 0_364 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3710000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3710000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(371000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3710000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3710000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3710000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3710000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [372] = function(args)
  -- function num : 0_365 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3720000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3720000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(372000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3720000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3720000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3720000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3720000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [373] = function(args)
  -- function num : 0_366 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3730000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3730000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(373000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3730000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3730000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3730000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3730000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [374] = function(args)
  -- function num : 0_367 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3740000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3740000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(374000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3740000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3740000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3740000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3740000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [375] = function(args)
  -- function num : 0_368 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3750000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3750000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(375000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3750000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3750000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3750000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3750000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [376] = function(args)
  -- function num : 0_369 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3760000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3760000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(376000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3760000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3760000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3760000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3760000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [377] = function(args)
  -- function num : 0_370 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3770000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
      effect:SetCfgID(3770000)
      root:AddEffect(effect)
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateThawEffect)()
          effect:SetCfgID(3770000)
          root:AddEffect(effect)
          if upper then
            upper:AddEffect(root)
          end
          return composed
        end
      end
    end
  end
end
, [378] = function(args)
  -- function num : 0_371 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3780000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3780000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(378000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateThawEffect)()
            effect:SetCfgID(3780000)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [379] = function(args)
  -- function num : 0_372 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3790000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3790000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(379000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3790000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3790000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
              effect:SetCfgID(3790000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3790000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [380] = function(args)
  -- function num : 0_373 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "", 0)
  effect:SetCfgID(3800000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "", 0)
      effect:SetCfgID(380000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(5, 26, 3, 1, true)
      effect:SetCfgID(3800000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(5, 26, 3, 1, true)
            effect:SetCfgID(380000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [381] = function(args)
  -- function num : 0_374 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "", 0)
  effect:SetCfgID(3810000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "", 0)
      effect:SetCfgID(381000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(5, 26, 3, 1, false)
      effect:SetCfgID(3810000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(5, 26, 3, 1, false)
            effect:SetCfgID(381000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [382] = function(args)
  -- function num : 0_375 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3820000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3820000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(382000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3820000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3820000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3820000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3820000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [383] = function(args)
  -- function num : 0_376 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3830000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3830000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(383000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3830000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3830000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3830000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3830000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [384] = function(args)
  -- function num : 0_377 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3840000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3840000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(384000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3840000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 188 2]")
              root:Add("[trap 188 3]")
              root:Add("[trap 188 4]")
              root:Add("[trap 188 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(3840000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 188 6]")
                    root:Add("[trap 188 7]")
                    root:Add("[trap 188 8]")
                    root:Add("[trap 188 9]")
                    root:Add("[trap 188 10]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(3840000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(3840000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [385] = function(args)
  -- function num : 0_378 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3850000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3850000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(385000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3850000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 188 2]")
              root:Add("[trap 188 3]")
              root:Add("[trap 188 4]")
              root:Add("[trap 188 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(3850000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 188 6]")
                    root:Add("[trap 188 7]")
                    root:Add("[trap 188 8]")
                    root:Add("[trap 188 9]")
                    root:Add("[trap 188 10]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(3850000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(3850000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [386] = function(args)
  -- function num : 0_379 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3860000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3860000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(386000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3860000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3860000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3860000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3860000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [387] = function(args)
  -- function num : 0_380 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3870000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3870000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(387000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(3870000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3870000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3870000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3870000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [388] = function(args)
  -- function num : 0_381 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3880000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
      effect:SetCfgID(3880000)
      root:AddEffect(effect)
      local upper = root or composed
      local root = nil
      if not composed then
        composed = (EffectFactory.CreateEffectList)()
        root = composed
      else
        root = (EffectFactory.CreateEffectList)()
      end
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(388000000)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(388000000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
            effect:SetCfgID(388000000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                effect:SetCfgID(388000000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                local upper = effect
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trigger", 1, "TowerIdle", 1)
                  effect:SetCfgID(3880000)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                    effect:SetCfgID(3880000)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(3880000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [389] = function(args)
  -- function num : 0_382 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(11, 18, 3, 1, true)
  effect:SetCfgID(3890000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(11, 18, 3, 1, true)
      effect:SetCfgID(389000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
      effect:SetCfgID(3890000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(389000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(3890000)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
              effect:SetCfgID(389000000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
              effect:SetCfgID(3890000)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
                  effect:SetCfgID(389000000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                  effect:SetCfgID(3890000)
                  effect:SetParam("runoninit")
                  local upper = effect
                  do
                    do
                      do
                        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                        effect:SetCfgID(389000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [390] = function(args)
  -- function num : 0_383 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(11, 18, 3, 1, true)
  effect:SetCfgID(3900000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateBlockChangeEffect)(11, 18, 3, 1, true)
        effect:SetCfgID(390000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [391] = function(args)
  -- function num : 0_384 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(11, 18, 3, 1, false)
  effect:SetCfgID(3910000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(11, 18, 3, 1, false)
      effect:SetCfgID(391000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateDestroyEffect)("[switch 190 9]")
        effect:SetCfgID(3910000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateDestroyEffect)("[sceneobject 25]")
            effect:SetCfgID(3910000)
            effect:SetParam("runoninit")
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [392] = function(args)
  -- function num : 0_385 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3920000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3920000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(392000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3920000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3920000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3920000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3920000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [393] = function(args)
  -- function num : 0_386 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3930000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3930000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(393000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(3930000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3930000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3930000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3930000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [394] = function(args)
  -- function num : 0_387 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3940000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3940000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(394000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3940000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3940000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3940000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3940000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [395] = function(args)
  -- function num : 0_388 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3950000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3950000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(395000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3950000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3950000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3950000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3950000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [396] = function(args)
  -- function num : 0_389 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3960000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3960000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(396000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(3960000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3960000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3960000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3960000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [397] = function(args)
  -- function num : 0_390 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3970000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3970000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(397000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(3970000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3970000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3970000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3970000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [398] = function(args)
  -- function num : 0_391 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3980000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3980000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(398000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(3980000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3980000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3980000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3980000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [399] = function(args)
  -- function num : 0_392 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3990000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3990000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(399000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(3990000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(3990000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(3990000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(3990000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [400] = function(args)
  -- function num : 0_393 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4000000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(4000000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(400000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(4000000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(4000000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(4000000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(4000000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [401] = function(args)
  -- function num : 0_394 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4010000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4010000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(401000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(4010000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(4010000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(4010000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(4010000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [402] = function(args)
  -- function num : 0_395 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4020000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(4020000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(402000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(4020000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(4020000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(4020000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(4020000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [403] = function(args)
  -- function num : 0_396 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4030000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4030000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(403000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(4030000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(4030000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(4030000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(4030000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [404] = function(args)
  -- function num : 0_397 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4040000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
      effect:SetCfgID(4040000)
      root:AddEffect(effect)
      local upper = root or composed
      local root = nil
      if not composed then
        composed = (EffectFactory.CreateEffectList)()
        root = composed
      else
        root = (EffectFactory.CreateEffectList)()
      end
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(404000000)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(404000000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
            effect:SetCfgID(404000000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                effect:SetCfgID(404000000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                local upper = effect
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trigger", 1, "TowerIdle", 1)
                  effect:SetCfgID(4040000)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                    effect:SetCfgID(4040000)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(4040000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [405] = function(args)
  -- function num : 0_398 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  do
    local root = nil
    if not composed then
      composed = (EffectFactory.CreateComposedEffect)()
      root = composed
    else
      root = (EffectFactory.CreateComposedEffect)()
    end
    if upper then
      upper:AddEffect(root)
    end
    return composed
  end
end
, [406] = function(args)
  -- function num : 0_399 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4060000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4060000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(406000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 208 5]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4060000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [407] = function(args)
  -- function num : 0_400 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(22, 9, 3, 1, true)
  effect:SetCfgID(4070000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(22, 9, 3, 1, true)
      effect:SetCfgID(407000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
      effect:SetCfgID(4070000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(407000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(4070000)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
              effect:SetCfgID(407000000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
              effect:SetCfgID(4070000)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
                  effect:SetCfgID(407000000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                  effect:SetCfgID(4070000)
                  effect:SetParam("runoninit")
                  local upper = effect
                  do
                    do
                      do
                        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                        effect:SetCfgID(407000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [408] = function(args)
  -- function num : 0_401 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(22, 9, 3, 1, true)
  effect:SetCfgID(4080000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateBlockChangeEffect)(22, 9, 3, 1, true)
        effect:SetCfgID(408000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [409] = function(args)
  -- function num : 0_402 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateEffectList)()
    root = composed
  else
    root = (EffectFactory.CreateEffectList)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(22, 9, 3, 1, false)
  effect:SetCfgID(4090000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(22, 9, 3, 1, false)
      effect:SetCfgID(409000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateDestroyEffect)("[switch 208 13]")
          effect:SetCfgID(4090000)
          effect:SetParam("runoninit")
          root:AddEffect(effect)
          if upper then
            upper:AddEffect(root)
          end
          return composed
        end
      end
    end
  end
end
, [410] = function(args)
  -- function num : 0_403 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4100000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
      effect:SetCfgID(4100000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
      effect:SetCfgID(4100000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(410000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
            effect:SetCfgID(4100000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4100000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [411] = function(args)
  -- function num : 0_404 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4110000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
      effect:SetCfgID(4110000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
      effect:SetCfgID(4110000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(411000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
            effect:SetCfgID(4110000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4110000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [412] = function(args)
  -- function num : 0_405 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4120000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
      effect:SetCfgID(4120000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
      effect:SetCfgID(4120000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(412000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
            effect:SetCfgID(4120000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4120000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [413] = function(args)
  -- function num : 0_406 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4130000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
      effect:SetCfgID(4130000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 1, "TowerOn4", 1)
      effect:SetCfgID(4130000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 1, "", 1)
          effect:SetCfgID(413000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
            effect:SetCfgID(4130000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4130000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [414] = function(args)
  -- function num : 0_407 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4140000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
      effect:SetCfgID(4140000)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
        effect:SetCfgID(4140000)
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateThawEffect)()
            effect:SetCfgID(4140000)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [415] = function(args)
  -- function num : 0_408 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4150000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
      effect:SetCfgID(4150000)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4150000)
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateThawEffect)()
            effect:SetCfgID(4150000)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [416] = function(args)
  -- function num : 0_409 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4160000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
      effect:SetCfgID(4160000)
      root:AddEffect(effect)
      local upper = root or composed
      local root = nil
      if not composed then
        composed = (EffectFactory.CreateEffectList)()
        root = composed
      else
        root = (EffectFactory.CreateEffectList)()
      end
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(416000000)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(416000000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
            effect:SetCfgID(416000000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                effect:SetCfgID(416000000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                local upper = effect
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trigger", 1, "TowerIdle", 1)
                  effect:SetCfgID(4160000)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                    effect:SetCfgID(4160000)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(4160000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [417] = function(args)
  -- function num : 0_410 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(5, 26, 3, 1, true)
  effect:SetCfgID(4170000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(5, 26, 3, 1, true)
      effect:SetCfgID(417000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
      effect:SetCfgID(4170000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(417000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(4170000)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
              effect:SetCfgID(417000000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
              effect:SetCfgID(4170000)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
                  effect:SetCfgID(417000000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                  effect:SetCfgID(4170000)
                  effect:SetParam("runoninit")
                  local upper = effect
                  do
                    do
                      do
                        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                        effect:SetCfgID(417000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [418] = function(args)
  -- function num : 0_411 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(5, 26, 3, 1, true)
  effect:SetCfgID(4180000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateBlockChangeEffect)(5, 26, 3, 1, true)
        effect:SetCfgID(418000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [419] = function(args)
  -- function num : 0_412 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateEffectList)()
    root = composed
  else
    root = (EffectFactory.CreateEffectList)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(5, 26, 3, 1, false)
  effect:SetCfgID(4190000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(5, 26, 3, 1, false)
      effect:SetCfgID(419000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateDestroyEffect)("[switch 209 13]")
          effect:SetCfgID(4190000)
          effect:SetParam("runoninit")
          root:AddEffect(effect)
          if upper then
            upper:AddEffect(root)
          end
          return composed
        end
      end
    end
  end
end
, [420] = function(args)
  -- function num : 0_413 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4200000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
      effect:SetCfgID(4200000)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
        effect:SetCfgID(4200000)
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateThawEffect)()
            effect:SetCfgID(4200000)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [421] = function(args)
  -- function num : 0_414 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4210000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
      effect:SetCfgID(4210000)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4210000)
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateThawEffect)()
            effect:SetCfgID(4210000)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [422] = function(args)
  -- function num : 0_415 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4220000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
      effect:SetCfgID(4220000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
      effect:SetCfgID(4220000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(422000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
            effect:SetCfgID(4220000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4220000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [423] = function(args)
  -- function num : 0_416 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4230000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
      effect:SetCfgID(4230000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
      effect:SetCfgID(4230000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(423000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
            effect:SetCfgID(4230000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4230000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [424] = function(args)
  -- function num : 0_417 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4240000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
      effect:SetCfgID(4240000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
      effect:SetCfgID(4240000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(424000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
            effect:SetCfgID(4240000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4240000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [425] = function(args)
  -- function num : 0_418 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4250000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
      effect:SetCfgID(4250000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 1, "TowerOn4", 1)
      effect:SetCfgID(4250000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 1, "", 1)
          effect:SetCfgID(425000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
            effect:SetCfgID(4250000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4250000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [426] = function(args)
  -- function num : 0_419 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4260000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(4260000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(426000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(4260000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 210 7]")
              root:Add("[trap 210 8]")
              root:Add("[trap 210 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(4260000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 210 2]")
                    root:Add("[trap 210 3]")
                    root:Add("[trap 210 4]")
                    root:Add("[trap 210 5]")
                    root:Add("[trap 210 6]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(4260000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(4260000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [427] = function(args)
  -- function num : 0_420 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4270000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4270000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(427000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(4270000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 210 7]")
              root:Add("[trap 210 8]")
              root:Add("[trap 210 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(4270000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 210 2]")
                    root:Add("[trap 210 3]")
                    root:Add("[trap 210 4]")
                    root:Add("[trap 210 5]")
                    root:Add("[trap 210 6]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(4270000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(4270000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [428] = function(args)
  -- function num : 0_421 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4280000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
      effect:SetCfgID(4280000)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
        effect:SetCfgID(4280000)
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateThawEffect)()
            effect:SetCfgID(4280000)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [429] = function(args)
  -- function num : 0_422 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4290000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
      effect:SetCfgID(4290000)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4290000)
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateThawEffect)()
            effect:SetCfgID(4290000)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [430] = function(args)
  -- function num : 0_423 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(28, 6, 3, 1, true)
  effect:SetCfgID(4300000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(28, 6, 3, 1, true)
      effect:SetCfgID(430000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
      effect:SetCfgID(4300000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(430000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(4300000)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
              effect:SetCfgID(430000000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
              effect:SetCfgID(4300000)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
                  effect:SetCfgID(430000000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                  effect:SetCfgID(4300000)
                  effect:SetParam("runoninit")
                  local upper = effect
                  do
                    do
                      do
                        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                        effect:SetCfgID(430000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [431] = function(args)
  -- function num : 0_424 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(28, 6, 3, 1, true)
  effect:SetCfgID(4310000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateBlockChangeEffect)(28, 6, 3, 1, true)
        effect:SetCfgID(431000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [432] = function(args)
  -- function num : 0_425 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateEffectList)()
    root = composed
  else
    root = (EffectFactory.CreateEffectList)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(28, 6, 3, 1, false)
  effect:SetCfgID(4320000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(28, 6, 3, 1, false)
      effect:SetCfgID(432000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateDestroyEffect)("[switch 211 13]")
          effect:SetCfgID(4320000)
          effect:SetParam("runoninit")
          root:AddEffect(effect)
          if upper then
            upper:AddEffect(root)
          end
          return composed
        end
      end
    end
  end
end
, [433] = function(args)
  -- function num : 0_426 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4330000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
      effect:SetCfgID(4330000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
      effect:SetCfgID(4330000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(433000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
            effect:SetCfgID(4330000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4330000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [434] = function(args)
  -- function num : 0_427 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4340000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
      effect:SetCfgID(4340000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
      effect:SetCfgID(4340000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(434000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
            effect:SetCfgID(4340000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4340000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [435] = function(args)
  -- function num : 0_428 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4350000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
      effect:SetCfgID(4350000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
      effect:SetCfgID(4350000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(435000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
            effect:SetCfgID(4350000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4350000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [436] = function(args)
  -- function num : 0_429 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4360000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
      effect:SetCfgID(4360000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 1, "TowerOn4", 1)
      effect:SetCfgID(4360000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 1, "", 1)
          effect:SetCfgID(436000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
            effect:SetCfgID(4360000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4360000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [437] = function(args)
  -- function num : 0_430 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
      effect:SetCfgID(4370000)
      root:AddEffect(effect)
      if upper then
        upper:AddEffect(root)
      end
      return composed
    end
  end
end
, [438] = function(args)
  -- function num : 0_431 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4380000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4380000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(438000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 209 5]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4380000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [439] = function(args)
  -- function num : 0_432 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4390000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4390000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(439000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 209 6]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4390000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [440] = function(args)
  -- function num : 0_433 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4400000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4400000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(440000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 209 7]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4400000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [441] = function(args)
  -- function num : 0_434 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4410000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4410000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(441000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 209 8]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4410000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [442] = function(args)
  -- function num : 0_435 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4420000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4420000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(442000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 208 5]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4420000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [443] = function(args)
  -- function num : 0_436 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4430000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4430000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(443000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 208 6]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4430000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [444] = function(args)
  -- function num : 0_437 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4440000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4440000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(444000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 208 7]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4440000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [445] = function(args)
  -- function num : 0_438 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4450000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4450000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(445000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 208 8]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4450000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [446] = function(args)
  -- function num : 0_439 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4460000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4460000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(446000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 211 5]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4460000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [447] = function(args)
  -- function num : 0_440 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4470000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4470000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(447000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 211 6]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4470000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [448] = function(args)
  -- function num : 0_441 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4480000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4480000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(448000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 211 7]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4480000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [449] = function(args)
  -- function num : 0_442 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4490000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4490000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(449000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 211 8]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4490000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [450] = function(args)
  -- function num : 0_443 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4500000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(4500000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(450000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(4500000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(4500000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(4500000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(4500000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [451] = function(args)
  -- function num : 0_444 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4510000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4510000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(451000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(4510000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(4510000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(4510000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(4510000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [452] = function(args)
  -- function num : 0_445 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4520000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
      effect:SetCfgID(4520000)
      root:AddEffect(effect)
      local upper = root or composed
      local root = nil
      if not composed then
        composed = (EffectFactory.CreateEffectList)()
        root = composed
      else
        root = (EffectFactory.CreateEffectList)()
      end
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(452000000)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(452000000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
            effect:SetCfgID(452000000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                effect:SetCfgID(452000000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                local upper = effect
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trigger", 1, "TowerIdle", 1)
                  effect:SetCfgID(4520000)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                    effect:SetCfgID(4520000)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(4520000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [453] = function(args)
  -- function num : 0_446 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4530000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4530000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(453000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 171 5]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4530000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [454] = function(args)
  -- function num : 0_447 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4540000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4540000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(454000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 171 6]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4540000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [455] = function(args)
  -- function num : 0_448 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4550000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4550000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(455000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 171 7]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4550000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [456] = function(args)
  -- function num : 0_449 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4560000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4560000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(456000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 171 8]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4560000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [457] = function(args)
  -- function num : 0_450 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(24, 10, 3, 1, true)
  effect:SetCfgID(4570000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(24, 10, 3, 1, true)
      effect:SetCfgID(457000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
      effect:SetCfgID(4570000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(457000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(4570000)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
              effect:SetCfgID(457000000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
              effect:SetCfgID(4570000)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
                  effect:SetCfgID(457000000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                  effect:SetCfgID(4570000)
                  effect:SetParam("runoninit")
                  local upper = effect
                  do
                    do
                      do
                        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                        effect:SetCfgID(457000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [458] = function(args)
  -- function num : 0_451 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(24, 10, 3, 1, true)
  effect:SetCfgID(4580000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateBlockChangeEffect)(24, 10, 3, 1, true)
        effect:SetCfgID(458000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [459] = function(args)
  -- function num : 0_452 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateEffectList)()
    root = composed
  else
    root = (EffectFactory.CreateEffectList)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(24, 10, 3, 1, false)
  effect:SetCfgID(4590000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(24, 10, 3, 1, false)
      effect:SetCfgID(459000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateDestroyEffect)("[switch 171 13]")
          effect:SetCfgID(4590000)
          effect:SetParam("runoninit")
          root:AddEffect(effect)
          if upper then
            upper:AddEffect(root)
          end
          return composed
        end
      end
    end
  end
end
, [460] = function(args)
  -- function num : 0_453 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4600000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
      effect:SetCfgID(4600000)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
        effect:SetCfgID(4600000)
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateThawEffect)()
            effect:SetCfgID(4600000)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [461] = function(args)
  -- function num : 0_454 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4610000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
      effect:SetCfgID(4610000)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4610000)
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateThawEffect)()
            effect:SetCfgID(4610000)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [462] = function(args)
  -- function num : 0_455 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4620000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
      effect:SetCfgID(4620000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
      effect:SetCfgID(4620000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(462000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
            effect:SetCfgID(4620000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4620000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [463] = function(args)
  -- function num : 0_456 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4630000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
      effect:SetCfgID(4630000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
      effect:SetCfgID(4630000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(463000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
            effect:SetCfgID(4630000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4630000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [464] = function(args)
  -- function num : 0_457 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4640000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
      effect:SetCfgID(4640000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
      effect:SetCfgID(4640000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(464000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
            effect:SetCfgID(4640000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4640000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [465] = function(args)
  -- function num : 0_458 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4650000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
      effect:SetCfgID(4650000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 1, "TowerOn4", 1)
      effect:SetCfgID(4650000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 1, "", 1)
          effect:SetCfgID(465000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
            effect:SetCfgID(4650000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4650000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [466] = function(args)
  -- function num : 0_459 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4660000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
      effect:SetCfgID(4660000)
      root:AddEffect(effect)
      local upper = root or composed
      local root = nil
      if not composed then
        composed = (EffectFactory.CreateEffectList)()
        root = composed
      else
        root = (EffectFactory.CreateEffectList)()
      end
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(466000000)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(466000000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
            effect:SetCfgID(466000000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                effect:SetCfgID(466000000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                local upper = effect
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trigger", 1, "TowerIdle", 1)
                  effect:SetCfgID(4660000)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
                    effect:SetCfgID(4660000)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(4660000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [467] = function(args)
  -- function num : 0_460 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4670000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4670000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(467000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 168 5]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4670000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [468] = function(args)
  -- function num : 0_461 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4680000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4680000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(468000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 168 6]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4680000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [469] = function(args)
  -- function num : 0_462 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4690000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4690000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(469000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 168 7]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4690000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [470] = function(args)
  -- function num : 0_463 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4700000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4700000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(470000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[switch 168 8]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4700000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [471] = function(args)
  -- function num : 0_464 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(34, 12, 3, 1, true)
  effect:SetCfgID(4710000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(34, 12, 3, 1, true)
      effect:SetCfgID(471000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
      effect:SetCfgID(4710000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(471000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(4710000)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 0, "TowerOff2", 1)
              effect:SetCfgID(471000000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
              effect:SetCfgID(4710000)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 0, "TowerOff3", 1)
                  effect:SetCfgID(471000000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                  effect:SetCfgID(4710000)
                  effect:SetParam("runoninit")
                  local upper = effect
                  do
                    do
                      do
                        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 0, "TowerOff4", 1)
                        effect:SetCfgID(471000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [472] = function(args)
  -- function num : 0_465 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(34, 12, 3, 1, true)
  effect:SetCfgID(4720000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateBlockChangeEffect)(34, 12, 3, 1, true)
        effect:SetCfgID(472000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [473] = function(args)
  -- function num : 0_466 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateEffectList)()
    root = composed
  else
    root = (EffectFactory.CreateEffectList)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateBlockChangeEffect)(34, 12, 3, 1, false)
  effect:SetCfgID(4730000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateBlockChangeEffect)(34, 12, 3, 1, false)
      effect:SetCfgID(473000000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateDestroyEffect)("[switch 168 13]")
          effect:SetCfgID(4730000)
          effect:SetParam("runoninit")
          root:AddEffect(effect)
          if upper then
            upper:AddEffect(root)
          end
          return composed
        end
      end
    end
  end
end
, [474] = function(args)
  -- function num : 0_467 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4740000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
      effect:SetCfgID(4740000)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
        effect:SetCfgID(4740000)
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateThawEffect)()
            effect:SetCfgID(4740000)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [475] = function(args)
  -- function num : 0_468 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4750000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
      effect:SetCfgID(4750000)
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4750000)
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateThawEffect)()
            effect:SetCfgID(4750000)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [476] = function(args)
  -- function num : 0_469 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4760000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
      effect:SetCfgID(4760000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
      effect:SetCfgID(4760000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(476000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
            effect:SetCfgID(4760000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4760000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [477] = function(args)
  -- function num : 0_470 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4770000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
      effect:SetCfgID(4770000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
      effect:SetCfgID(4770000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(477000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
            effect:SetCfgID(4770000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4770000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [478] = function(args)
  -- function num : 0_471 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4780000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
      effect:SetCfgID(4780000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
      effect:SetCfgID(4780000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(478000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
            effect:SetCfgID(4780000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4780000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [479] = function(args)
  -- function num : 0_472 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4790000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
      effect:SetCfgID(4790000)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 1, "TowerOn4", 1)
      effect:SetCfgID(4790000)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal4", 1, "", 1)
          effect:SetCfgID(479000000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
            effect:SetCfgID(4790000)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(4790000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [480] = function(args)
  -- function num : 0_473 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4800000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(4800000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(480000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (8)", true)
        effect:SetCfgID(4800000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (8)", true)
            effect:SetCfgID(480000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)(false)
                effect:SetCfgID(4800000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [481] = function(args)
  -- function num : 0_474 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4810000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(4810000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(481000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (9)", true)
        effect:SetCfgID(4810000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (9)", true)
            effect:SetCfgID(481000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (4)", false)
            effect:SetCfgID(4810000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (4)", false)
                effect:SetCfgID(481000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)(false)
                    effect:SetCfgID(4810000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [482] = function(args)
  -- function num : 0_475 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4820000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(4820000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(482000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (17)", true)
        effect:SetCfgID(4820000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (17)", true)
            effect:SetCfgID(482000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
            effect:SetCfgID(4820000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
                effect:SetCfgID(482000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)(false)
                    effect:SetCfgID(4820000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [483] = function(args)
  -- function num : 0_476 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4830000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(4830000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(483000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (18)", true)
        effect:SetCfgID(4830000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (18)", true)
            effect:SetCfgID(483000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)(false)
                effect:SetCfgID(4830000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [484] = function(args)
  -- function num : 0_477 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4840000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(4840000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(484000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (19)", true)
        effect:SetCfgID(4840000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (19)", true)
            effect:SetCfgID(484000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
            effect:SetCfgID(4840000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
                effect:SetCfgID(484000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)(false)
                    effect:SetCfgID(4840000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [485] = function(args)
  -- function num : 0_478 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4850000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(4850000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(485000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (20)", true)
        effect:SetCfgID(4850000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (20)", true)
            effect:SetCfgID(485000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (13)", false)
            effect:SetCfgID(4850000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (13)", false)
                effect:SetCfgID(485000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
                effect:SetCfgID(4850000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
                    effect:SetCfgID(485000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)(false)
                        effect:SetCfgID(4850000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [486] = function(args)
  -- function num : 0_479 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4860000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(4860000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(486000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (21)", true)
        effect:SetCfgID(4860000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (21)", true)
            effect:SetCfgID(486000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (14)", false)
            effect:SetCfgID(4860000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (14)", false)
                effect:SetCfgID(486000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (13)", true)
                effect:SetCfgID(4860000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (13)", true)
                    effect:SetCfgID(486000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
                    effect:SetCfgID(4860000)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
                        effect:SetCfgID(486000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)(false)
                            effect:SetCfgID(4860000)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [487] = function(args)
  -- function num : 0_480 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4870000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(4870000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(487000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (22)", true)
        effect:SetCfgID(4870000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (22)", true)
            effect:SetCfgID(487000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)(false)
                effect:SetCfgID(4870000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [488] = function(args)
  -- function num : 0_481 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4880000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(4880000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(488000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", true)
        effect:SetCfgID(4880000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", true)
            effect:SetCfgID(488000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
            effect:SetCfgID(4880000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
                effect:SetCfgID(488000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)(false)
                    effect:SetCfgID(4880000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [489] = function(args)
  -- function num : 0_482 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4890000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(4890000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(489000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (6)", true)
        effect:SetCfgID(4890000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (6)", true)
            effect:SetCfgID(489000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
            effect:SetCfgID(4890000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
                effect:SetCfgID(489000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)(false)
                    effect:SetCfgID(4890000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [490] = function(args)
  -- function num : 0_483 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4900000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(4900000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(490000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(4900000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
            effect:SetCfgID(490000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
            effect:SetCfgID(4900000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
                effect:SetCfgID(490000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)(false)
                    effect:SetCfgID(4900000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [491] = function(args)
  -- function num : 0_484 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4910000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(4910000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(491000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (10)", true)
        effect:SetCfgID(4910000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (10)", true)
            effect:SetCfgID(491000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", false)
            effect:SetCfgID(4910000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", false)
                effect:SetCfgID(491000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (11)", true)
                effect:SetCfgID(4910000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (11)", true)
                    effect:SetCfgID(491000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)(false)
                        effect:SetCfgID(4910000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [492] = function(args)
  -- function num : 0_485 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4920000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(4920000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(492000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (12)", true)
        effect:SetCfgID(4920000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (12)", true)
            effect:SetCfgID(492000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (13)", true)
            effect:SetCfgID(4920000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (13)", true)
                effect:SetCfgID(492000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (5)", false)
                effect:SetCfgID(4920000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (5)", false)
                    effect:SetCfgID(492000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (4)", false)
                    effect:SetCfgID(4920000)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (4)", false)
                        effect:SetCfgID(492000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)(false)
                            effect:SetCfgID(4920000)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [493] = function(args)
  -- function num : 0_486 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4930000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(4930000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(493000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (14)", true)
        effect:SetCfgID(4930000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (14)", true)
            effect:SetCfgID(493000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (15)", true)
            effect:SetCfgID(4930000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (15)", true)
                effect:SetCfgID(493000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (6)", false)
                effect:SetCfgID(4930000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (6)", false)
                    effect:SetCfgID(493000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (7)", false)
                    effect:SetCfgID(4930000)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (7)", false)
                        effect:SetCfgID(493000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)(false)
                            effect:SetCfgID(4930000)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [494] = function(args)
  -- function num : 0_487 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4940000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 122 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(4940000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 122 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(494000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (23)", true)
        effect:SetCfgID(4940000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (23)", true)
            effect:SetCfgID(494000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (24)", true)
            effect:SetCfgID(4940000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (24)", true)
                effect:SetCfgID(494000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
                effect:SetCfgID(4940000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
                    effect:SetCfgID(494000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
                    effect:SetCfgID(4940000)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
                        effect:SetCfgID(494000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)(false)
                            effect:SetCfgID(4940000)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [495] = function(args)
  -- function num : 0_488 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4950000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 135 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(4950000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 135 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(495000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (25)", true)
        effect:SetCfgID(4950000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (25)", true)
            effect:SetCfgID(495000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (26)", true)
            effect:SetCfgID(4950000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (26)", true)
                effect:SetCfgID(495000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
                effect:SetCfgID(4950000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
                    effect:SetCfgID(495000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
                    effect:SetCfgID(4950000)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
                        effect:SetCfgID(495000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)(false)
                            effect:SetCfgID(4950000)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [496] = function(args)
  -- function num : 0_489 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4960000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 157 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(4960000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 157 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(496000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(4960000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
            effect:SetCfgID(496000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
            effect:SetCfgID(4960000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
                effect:SetCfgID(496000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
                effect:SetCfgID(4960000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
                    effect:SetCfgID(496000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
                    effect:SetCfgID(4960000)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
                        effect:SetCfgID(496000000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)()
                            effect:SetCfgID(4960000)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [497] = function(args)
  -- function num : 0_490 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4970000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 157 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(4970000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 157 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(497000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(4970000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
            effect:SetCfgID(497000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
            effect:SetCfgID(4970000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
                effect:SetCfgID(497000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (10)", true)
                effect:SetCfgID(4970000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (10)", true)
                    effect:SetCfgID(497000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(4970000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [498] = function(args)
  -- function num : 0_491 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4980000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 163 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(4980000)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 163 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(498000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(4980000)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
            effect:SetCfgID(498000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
            effect:SetCfgID(4980000)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
                effect:SetCfgID(498000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", true)
                effect:SetCfgID(4980000)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (5)", true)
                    effect:SetCfgID(498000000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(4980000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [499] = function(args)
  -- function num : 0_492 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(4990000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(4990000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(499000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(4990000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(4990000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(4990000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)(false)
                  effect:SetCfgID(4990000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [500] = function(args)
  -- function num : 0_493 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(5000000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0.25)
    effect:SetCfgID(5000000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(500000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(5000000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(5000000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(5000000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)(false)
                  effect:SetCfgID(5000000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [501] = function(args)
  -- function num : 0_494 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(5010000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(5010000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(501000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(5010000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(5010000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(5010000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)(false)
                  effect:SetCfgID(5010000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [502] = function(args)
  -- function num : 0_495 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(5020000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0.25)
    effect:SetCfgID(5020000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(502000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(5020000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(5020000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(5020000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)(false)
                  effect:SetCfgID(5020000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [503] = function(args)
  -- function num : 0_496 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(5030000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(5030000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(503000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(5030000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(5030000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(5030000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)(false)
                  effect:SetCfgID(5030000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [504] = function(args)
  -- function num : 0_497 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(5040000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0.25)
    effect:SetCfgID(5040000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(504000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(5040000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(5040000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(5040000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)(false)
                  effect:SetCfgID(5040000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [505] = function(args)
  -- function num : 0_498 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(5050000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(5050000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(505000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(5050000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(5050000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(5050000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)(false)
                  effect:SetCfgID(5050000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [506] = function(args)
  -- function num : 0_499 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(5060000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0.25)
    effect:SetCfgID(5060000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(506000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(5060000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(5060000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(5060000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)(false)
                  effect:SetCfgID(5060000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [507] = function(args)
  -- function num : 0_500 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(5070000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(5070000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(507000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(5070000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(5070000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(5070000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)(false)
                  effect:SetCfgID(5070000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [508] = function(args)
  -- function num : 0_501 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(5080000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0.25)
    effect:SetCfgID(5080000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(508000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(5080000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(5080000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(5080000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)(false)
                  effect:SetCfgID(5080000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [509] = function(args)
  -- function num : 0_502 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(5090000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(5090000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(509000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(5090000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(5090000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(5090000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)(false)
                  effect:SetCfgID(5090000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [510] = function(args)
  -- function num : 0_503 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)(false)
    effect:SetCfgID(5100000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0.25)
    effect:SetCfgID(5100000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(510000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(5100000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(5100000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(5100000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)(false)
                  effect:SetCfgID(5100000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [511] = function(args)
  -- function num : 0_504 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5110000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 222 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
    effect:SetCfgID(5110000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 222 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(511000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(5110000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
            effect:SetCfgID(511000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(5110000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [512] = function(args)
  -- function num : 0_505 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5120000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 222 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(5120000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 222 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(512000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(5120000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
            effect:SetCfgID(512000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(5120000)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [513] = function(args)
  -- function num : 0_506 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5130000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 222 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(5130000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 222 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(513000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
        effect:SetCfgID(5130000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
            effect:SetCfgID(513000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
            effect:SetCfgID(5130000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
                effect:SetCfgID(513000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(5130000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [514] = function(args)
  -- function num : 0_507 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5140000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 222 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(5140000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 222 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(514000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(5140000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
            effect:SetCfgID(514000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
            effect:SetCfgID(5140000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
                effect:SetCfgID(514000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(5140000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [515] = function(args)
  -- function num : 0_508 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5150000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 222 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(5150000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 222 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(515000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(5150000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
            effect:SetCfgID(515000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
            effect:SetCfgID(5150000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
                effect:SetCfgID(515000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(5150000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [516] = function(args)
  -- function num : 0_509 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5160000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 222 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(5160000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 222 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(516000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
        effect:SetCfgID(5160000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
            effect:SetCfgID(516000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
            effect:SetCfgID(5160000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
                effect:SetCfgID(516000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(5160000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [517] = function(args)
  -- function num : 0_510 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5170000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 222 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(5170000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 222 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(517000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
        effect:SetCfgID(5170000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
            effect:SetCfgID(517000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
            effect:SetCfgID(5170000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
                effect:SetCfgID(517000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(5170000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [518] = function(args)
  -- function num : 0_511 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5180000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 222 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(5180000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 222 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(518000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
        effect:SetCfgID(5180000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
            effect:SetCfgID(518000000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
            effect:SetCfgID(5180000)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
                effect:SetCfgID(518000000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(5180000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [519] = function(args)
  -- function num : 0_0 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "SwitchOff", 0.25)
  effect:SetCfgID(10100)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(1010000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [520] = function(args)
  -- function num : 0_1 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "SwitchOn", 0.25)
  effect:SetCfgID(20200)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(2020000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [521] = function(args)
  -- function num : 0_2 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(30300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
    effect:SetCfgID(30300)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(3030000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(30300)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", false)
            effect:SetCfgID(3030000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (10)", false)
            effect:SetCfgID(30300)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (10)", false)
                effect:SetCfgID(3030000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (11)", false)
                effect:SetCfgID(30300)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (11)", false)
                    effect:SetCfgID(3030000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(30303)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [522] = function(args)
  -- function num : 0_3 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(40400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(40400)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(4040000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(40400)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
            effect:SetCfgID(4040000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(40404)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [523] = function(args)
  -- function num : 0_4 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(50500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "", 0)
    effect:SetCfgID(50500)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(5050000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapCheckEffect)(args.traps, args.switches)
          effect:SetCfgID(50500)
          effect:SetParam("runoninit")
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(50505)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [524] = function(args)
  -- function num : 0_5 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(60600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 2, "", 0)
    effect:SetCfgID(60600)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 2, "", 0)
        effect:SetCfgID(6060000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapCheckEffect)(args.traps, args.switches)
          effect:SetCfgID(60600)
          effect:SetParam("runoninit")
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(60606)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [525] = function(args)
  -- function num : 0_6 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(70700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 3, "", 0)
    effect:SetCfgID(70700)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 3, "", 0)
        effect:SetCfgID(7070000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapCheckEffect)(args.traps, args.switches)
          effect:SetCfgID(70700)
          effect:SetParam("runoninit")
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(70707)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [526] = function(args)
  -- function num : 0_7 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(80800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 4, "", 0)
    effect:SetCfgID(80800)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 4, "", 0)
        effect:SetCfgID(8080000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapCheckEffect)(args.traps, args.switches)
          effect:SetCfgID(80800)
          effect:SetParam("runoninit")
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(80808)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [527] = function(args)
  -- function num : 0_8 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(90900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
    effect:SetCfgID(90900)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(9090000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(90900)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (3)", false)
            effect:SetCfgID(9090000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (12)", false)
            effect:SetCfgID(90900)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (12)", false)
                effect:SetCfgID(9090000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (13)", false)
                effect:SetCfgID(90900)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (13)", false)
                    effect:SetCfgID(9090000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(90909)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [528] = function(args)
  -- function num : 0_9 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1001000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(1001000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(100100000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(1001000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (3)", true)
            effect:SetCfgID(100100000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(10010010)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [529] = function(args)
  -- function num : 0_10 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1101100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1101100)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(110110000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(1101100)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (2)", false)
            effect:SetCfgID(110110000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(11011011)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [530] = function(args)
  -- function num : 0_11 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1201200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1201200)
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(120120000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (2)", true)
        effect:SetCfgID(1201200)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (2)", true)
            effect:SetCfgID(120120000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (10)", false)
            effect:SetCfgID(1201200)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (10)", false)
                effect:SetCfgID(120120000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
                effect:SetCfgID(1201200)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
                    effect:SetCfgID(120120000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (11)", false)
                    effect:SetCfgID(1201200)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (11)", false)
                        effect:SetCfgID(120120000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)()
                            effect:SetCfgID(12012012)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [531] = function(args)
  -- function num : 0_12 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1301300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1301300)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(130130000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (4)", false)
        effect:SetCfgID(1301300)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (4)", false)
            effect:SetCfgID(130130000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
            effect:SetCfgID(1301300)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
                effect:SetCfgID(130130000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (9)", false)
                effect:SetCfgID(1301300)
                effect:SetParam("runoninit")
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (9)", false)
                    effect:SetCfgID(130130000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(13013013)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [532] = function(args)
  -- function num : 0_13 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1401400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1401400)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(140140000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (4)", true)
        effect:SetCfgID(1401400)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (4)", true)
            effect:SetCfgID(140140000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (9)", false)
            effect:SetCfgID(1401400)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (9)", false)
                effect:SetCfgID(140140000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (12)", false)
                effect:SetCfgID(1401400)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (12)", false)
                    effect:SetCfgID(140140000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (13)", false)
                    effect:SetCfgID(1401400)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (13)", false)
                        effect:SetCfgID(140140000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateThawEffect)()
                            effect:SetCfgID(14014014)
                            root:AddEffect(effect)
                            if upper then
                              upper:AddEffect(root)
                            end
                            return composed
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [533] = function(args)
  -- function num : 0_14 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1501500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1501500)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(150150000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (5)", false)
        effect:SetCfgID(1501500)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (5)", false)
            effect:SetCfgID(150150000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(15015015)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [534] = function(args)
  -- function num : 0_15 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1601600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1601600)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(160160000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (5)", true)
        effect:SetCfgID(1601600)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (5)", true)
            effect:SetCfgID(160160000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(1601600)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [535] = function(args)
  -- function num : 0_16 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1701700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1701700)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(170170000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (6)", false)
        effect:SetCfgID(1701700)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (6)", false)
            effect:SetCfgID(170170000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
            effect:SetCfgID(1701700)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
                effect:SetCfgID(170170000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (8)", false)
                effect:SetCfgID(1701700)
                effect:SetParam("runoninit")
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (8)", false)
                    effect:SetCfgID(170170000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(17017017)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [536] = function(args)
  -- function num : 0_17 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1801800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
    effect:SetCfgID(1801800)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(180180000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (8)", false)
        effect:SetCfgID(1801800)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (8)", false)
            effect:SetCfgID(180180000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (10)", false)
            effect:SetCfgID(1801800)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (10)", false)
                effect:SetCfgID(180180000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
                effect:SetCfgID(1801800)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
                    effect:SetCfgID(180180000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (11)", false)
                    effect:SetCfgID(1801800)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (11)", false)
                        effect:SetCfgID(180180000)
                        upper:SetParam("alternative", effect)
                        root:AddEffect(effect)
                        local upper = effect
                        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (14)", false)
                        effect:SetCfgID(1801800)
                        effect:SetParam("runoninit")
                        local upper = effect
                        do
                          do
                            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (14)", false)
                            effect:SetCfgID(180180000)
                            upper:SetParam("alternative", effect)
                            root:AddEffect(effect)
                            local upper = effect
                            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (15)", false)
                            effect:SetCfgID(1801800)
                            effect:SetParam("runoninit")
                            local upper = effect
                            do
                              do
                                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (15)", false)
                                effect:SetCfgID(180180000)
                                upper:SetParam("alternative", effect)
                                root:AddEffect(effect)
                                local upper = effect
                                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (6)", true)
                                effect:SetCfgID(18018018)
                                effect:SetParam("runoninit")
                                local upper = effect
                                do
                                  do
                                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (6)", true)
                                    effect:SetCfgID(1801801800)
                                    upper:SetParam("alternative", effect)
                                    root:AddEffect(effect)
                                    local upper = effect
                                    do
                                      do
                                        local effect = (EffectFactory.CreateThawEffect)()
                                        effect:SetCfgID(18018018)
                                        root:AddEffect(effect)
                                        if upper then
                                          upper:AddEffect(root)
                                        end
                                        return composed
                                      end
                                    end
                                  end
                                end
                              end
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [537] = function(args)
  -- function num : 0_18 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1901900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(1901900)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(190190000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (7)", false)
        effect:SetCfgID(1901900)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (7)", false)
            effect:SetCfgID(190190000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (14)", false)
            effect:SetCfgID(1901900)
            effect:SetParam("runoninit")
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (14)", false)
                effect:SetCfgID(190190000)
                upper:SetParam("alternative", effect)
                root:AddEffect(effect)
                local upper = effect
                local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (15)", false)
                effect:SetCfgID(1901900)
                effect:SetParam("runoninit")
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (15)", false)
                    effect:SetCfgID(190190000)
                    upper:SetParam("alternative", effect)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(19019019)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [538] = function(args)
  -- function num : 0_19 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2002000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(2002000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(200200000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (7)", true)
        effect:SetCfgID(2002000)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (7)", true)
            effect:SetCfgID(200200000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(20020020)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [539] = function(args)
  -- function num : 0_20 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2102100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2102100)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(210210000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(2102100)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 3 4]")
              root:Add("[trap 3 2]")
              root:Add("[trap 3 3]")
              root:Add("[trap 3 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(2102100)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(21021021)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [540] = function(args)
  -- function num : 0_21 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2202200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2202200)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(220220000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(2202200)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 1 2]")
              root:Add("[trap 1 3]")
              root:Add("[trap 1 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(2202200)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(22022022)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [541] = function(args)
  -- function num : 0_22 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2302300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2302300)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(230230000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(2302300)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 1 2]")
              root:Add("[trap 1 3]")
              root:Add("[trap 1 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              local effect = (EffectFactory.CreateRemoveEventEffect)(100092)
              effect:SetCfgID(2302300)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateRemoveEventEffect)(100092)
                  effect:SetCfgID(230230000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                    effect:SetCfgID(2302300)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(23023023)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [542] = function(args)
  -- function num : 0_23 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2402400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2402400)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(240240000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(2402400)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 2 2]")
              root:Add("[trap 2 3]")
              root:Add("[trap 2 4]")
              root:Add("[trap 2 5]")
              root:Add("[trap 2 6]")
              root:Add("[trap 2 7]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(2402400)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(24024024)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [543] = function(args)
  -- function num : 0_24 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2502500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2502500)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(250250000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(2502500)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 2 2]")
              root:Add("[trap 2 3]")
              root:Add("[trap 2 4]")
              root:Add("[trap 2 5]")
              root:Add("[trap 2 6]")
              root:Add("[trap 2 7]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(2502500)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(25025025)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [544] = function(args)
  -- function num : 0_25 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2602600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2602600)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(260260000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[trap 4 2]")
            root:Add("[trap 4 3]")
            root:Add("[trap 4 4]")
            root:Add("[trap 4 5]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(26026026)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [545] = function(args)
  -- function num : 0_26 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2702700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2702700)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(270270000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[trap 4 2]")
            root:Add("[trap 4 3]")
            root:Add("[trap 4 4]")
            root:Add("[trap 4 5]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(27027027)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [546] = function(args)
  -- function num : 0_27 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2802800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(2802800)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(280280000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[trap 5 2]")
            root:Add("[trap 5 3]")
            root:Add("[trap 5 4]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(28028028)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [547] = function(args)
  -- function num : 0_28 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(2902900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(2902900)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(290290000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[trap 5 2]")
            root:Add("[trap 5 3]")
            root:Add("[trap 5 4]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(29029029)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [548] = function(args)
  -- function num : 0_29 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3003000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3003000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(300300000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3003000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 6 2]")
              root:Add("[trap 6 3]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(3003000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(30030030)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [549] = function(args)
  -- function num : 0_30 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3103100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3103100)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(310310000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(3103100)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 6 2]")
              root:Add("[trap 6 3]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(3103100)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(31031030)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [550] = function(args)
  -- function num : 0_31 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3203200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3203200)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(320320000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateBGMEffect)("play", 92)
          effect:SetCfgID(3203200)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_22", true)
          effect:SetCfgID(3203200)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_22", true)
              effect:SetCfgID(320320000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/01", false)
              effect:SetCfgID(3203200)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateVisualEffect)("EventItem/01", false)
                  effect:SetCfgID(320320000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
                    effect:SetCfgID(3203200)
                    root:AddEffect(effect)
                    local upper = root
                    local root = (ChangeTrapStateEffect.Create)()
                    local upper = root
                    do
                      do
                        local root = ((ChangeTrapStateEffect.Parallel).Create)()
                        root:Add("[trap 7 2]")
                        root:Add("[trap 7 3]")
                        root:Add("[trap 7 4]")
                        upper:Add(root)
                        upper:AddEffect(root)
                        local upper = effect
                        do
                          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                          effect:SetCfgID(3203200)
                          root:AddEffect(effect)
                          local upper = effect
                          do
                            do
                              local effect = (EffectFactory.CreateThawEffect)()
                              effect:SetCfgID(32032030)
                              root:AddEffect(effect)
                              if upper then
                                upper:AddEffect(root)
                              end
                              return composed
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [551] = function(args)
  -- function num : 0_32 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3303300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3303300)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(330330000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateBGMEffect)("play", 92)
          effect:SetCfgID(3303300)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_22", false)
          effect:SetCfgID(3303300)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_22", false)
              effect:SetCfgID(330330000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/01", true)
              effect:SetCfgID(3303300)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateVisualEffect)("EventItem/01", true)
                  effect:SetCfgID(330330000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
                    effect:SetCfgID(3303300)
                    root:AddEffect(effect)
                    local upper = root
                    local root = (ChangeTrapStateEffect.Create)()
                    local upper = root
                    do
                      do
                        local root = ((ChangeTrapStateEffect.Parallel).Create)()
                        root:Add("[trap 7 2]")
                        root:Add("[trap 7 3]")
                        root:Add("[trap 7 4]")
                        upper:Add(root)
                        upper:AddEffect(root)
                        local upper = effect
                        do
                          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                          effect:SetCfgID(3303300)
                          root:AddEffect(effect)
                          local upper = effect
                          do
                            do
                              local effect = (EffectFactory.CreateThawEffect)()
                              effect:SetCfgID(33033033)
                              root:AddEffect(effect)
                              if upper then
                                upper:AddEffect(root)
                              end
                              return composed
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [552] = function(args)
  -- function num : 0_33 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3403400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(3403400)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(340340000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateBGMEffect)("play", 92)
          effect:SetCfgID(3403400)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_23", true)
          effect:SetCfgID(3403400)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_23", true)
              effect:SetCfgID(340340000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/02", false)
              effect:SetCfgID(3403400)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateVisualEffect)("EventItem/02", false)
                  effect:SetCfgID(340340000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
                    effect:SetCfgID(3403400)
                    root:AddEffect(effect)
                    local upper = root
                    local root = (ChangeTrapStateEffect.Create)()
                    local upper = root
                    do
                      do
                        local root = ((ChangeTrapStateEffect.Parallel).Create)()
                        root:Add("[trap 8 2]")
                        root:Add("[trap 8 3]")
                        root:Add("[trap 8 4]")
                        upper:Add(root)
                        upper:AddEffect(root)
                        local upper = effect
                        do
                          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                          effect:SetCfgID(3403400)
                          root:AddEffect(effect)
                          local upper = effect
                          do
                            do
                              local effect = (EffectFactory.CreateThawEffect)()
                              effect:SetCfgID(34034034)
                              root:AddEffect(effect)
                              if upper then
                                upper:AddEffect(root)
                              end
                              return composed
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [553] = function(args)
  -- function num : 0_34 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3503500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(3503500)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(350350000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateBGMEffect)("play", 92)
          effect:SetCfgID(3503500)
          root:AddEffect(effect)
          local upper = effect
          local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_23", false)
          effect:SetCfgID(3503500)
          effect:SetParam("runoninit")
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/t1_m_obj_23", false)
              effect:SetCfgID(350350000)
              upper:SetParam("alternative", effect)
              root:AddEffect(effect)
              local upper = effect
              local effect = (EffectFactory.CreateVisualEffect)("EventItem/02", true)
              effect:SetCfgID(3503500)
              effect:SetParam("runoninit")
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateVisualEffect)("EventItem/02", true)
                  effect:SetCfgID(350350000)
                  upper:SetParam("alternative", effect)
                  root:AddEffect(effect)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
                    effect:SetCfgID(3503500)
                    root:AddEffect(effect)
                    local upper = root
                    local root = (ChangeTrapStateEffect.Create)()
                    local upper = root
                    do
                      do
                        local root = ((ChangeTrapStateEffect.Parallel).Create)()
                        root:Add("[trap 8 2]")
                        root:Add("[trap 8 3]")
                        root:Add("[trap 8 4]")
                        upper:Add(root)
                        upper:AddEffect(root)
                        local upper = effect
                        do
                          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                          effect:SetCfgID(3503500)
                          root:AddEffect(effect)
                          local upper = effect
                          do
                            do
                              local effect = (EffectFactory.CreateThawEffect)()
                              effect:SetCfgID(35035035)
                              root:AddEffect(effect)
                              if upper then
                                upper:AddEffect(root)
                              end
                              return composed
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [554] = function(args)
  -- function num : 0_35 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3603600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(3603600)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(360360000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(3603600)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (1)", true)
            effect:SetCfgID(360360000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(36036036)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [555] = function(args)
  -- function num : 0_36 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3703700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
    effect:SetCfgID(3703700)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(370370000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(3703700)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (3)", true)
            effect:SetCfgID(370370000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(37037037)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [556] = function(args)
  -- function num : 0_37 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(3803800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateVisualEffect)("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
    effect:SetCfgID(3803800)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateVisualEffect)("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(380380000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (7)", false)
        effect:SetCfgID(3803800)
        effect:SetParam("runoninit")
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateVisualEffect)("Particles/Sence_TX_Prefab_JiGuang (7)", false)
            effect:SetCfgID(380380000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            local upper = effect
            do
              do
                local effect = (EffectFactory.CreateThawEffect)()
                effect:SetCfgID(38038038)
                root:AddEffect(effect)
                if upper then
                  upper:AddEffect(root)
                end
                return composed
              end
            end
          end
        end
      end
    end
  end
end
, [557] = function(args)
  -- function num : 0_38 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "GoUp", 0.25)
  effect:SetCfgID(3903900)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(390390000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [558] = function(args)
  -- function num : 0_39 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "GoDown", 0.25)
  effect:SetCfgID(4004000)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(400400000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [559] = function(args)
  -- function num : 0_40 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "DoorOpen", 0.1)
  effect:SetCfgID(4204200)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
      effect:SetCfgID(420420000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(3, 15, 1, 1, false)
      effect:SetCfgID(4204200)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateBlockChangeEffect)(3, 15, 1, 1, false)
          effect:SetCfgID(420420000)
          upper:SetParam("alternative", effect)
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateRemoveEventEffect)(100627)
              effect:SetCfgID(4204200)
              effect:SetParam("runoninit")
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [560] = function(args)
  -- function num : 0_41 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "DoorOpen", 0.1)
  effect:SetCfgID(4304300)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
      effect:SetCfgID(430430000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(14, 11, 1, 1, false)
      effect:SetCfgID(4304300)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(14, 11, 1, 1, false)
            effect:SetCfgID(430430000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [561] = function(args)
  -- function num : 0_42 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "DoorClose", 0.1)
  effect:SetCfgID(4404400)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
      effect:SetCfgID(440440000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(3, 15, 1, 1, true)
      effect:SetCfgID(4404400)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(3, 15, 1, 1, true)
            effect:SetCfgID(440440000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [562] = function(args)
  -- function num : 0_43 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "DoorClose", 0.1)
  effect:SetCfgID(4604600)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
      effect:SetCfgID(460460000)
      upper:SetParam("alternative", effect)
      root:AddEffect(effect)
      local upper = effect
      local effect = (EffectFactory.CreateBlockChangeEffect)(14, 11, 1, 1, true)
      effect:SetCfgID(4604600)
      effect:SetParam("runoninit")
      local upper = effect
      do
        do
          do
            local effect = (EffectFactory.CreateBlockChangeEffect)(14, 11, 1, 1, true)
            effect:SetCfgID(460460000)
            upper:SetParam("alternative", effect)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [563] = function(args)
  -- function num : 0_44 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4704700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(4704700)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(470470000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(4704700)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 3 4]")
              root:Add("[trap 3 2]")
              root:Add("[trap 3 3]")
              root:Add("[trap 3 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(4704700)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(4704700)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [564] = function(args)
  -- function num : 0_45 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4804800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(4804801)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(480480100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(4804802)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 11 2]")
              root:Add("[trap 11 3]")
              root:Add("[trap 11 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(4804804)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(4804805)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [565] = function(args)
  -- function num : 0_46 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(4904900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(4904901)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(490490100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(4904902)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 11 2]")
              root:Add("[trap 11 3]")
              root:Add("[trap 11 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(4904904)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(4904905)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [566] = function(args)
  -- function num : 0_47 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5005000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(5005001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(500500100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(5005002)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 12 2]")
              root:Add("[trap 12 3]")
              root:Add("[trap 12 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                effect:SetCfgID(5005004)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 12 5]")
                    root:Add("[trap 12 6]")
                    root:Add("[trap 12 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
                      effect:SetCfgID(5005006)
                      root:AddEffect(effect)
                      local upper = root
                      local root = (ChangeTrapStateEffect.Create)()
                      local upper = root
                      do
                        do
                          local root = ((ChangeTrapStateEffect.Parallel).Create)()
                          root:Add("[trap 12 8]")
                          root:Add("[trap 12 9]")
                          root:Add("[trap 12 10]")
                          upper:Add(root)
                          upper:AddEffect(root)
                          local upper = effect
                          do
                            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                            effect:SetCfgID(5005008)
                            root:AddEffect(effect)
                            local upper = effect
                            do
                              do
                                local effect = (EffectFactory.CreateThawEffect)()
                                effect:SetCfgID(5005009)
                                root:AddEffect(effect)
                                if upper then
                                  upper:AddEffect(root)
                                end
                                return composed
                              end
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [567] = function(args)
  -- function num : 0_48 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5105100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(5105101)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(510510100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(5105102)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 12 2]")
              root:Add("[trap 12 3]")
              root:Add("[trap 12 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                effect:SetCfgID(5105104)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 12 5]")
                    root:Add("[trap 12 6]")
                    root:Add("[trap 12 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
                      effect:SetCfgID(5105106)
                      root:AddEffect(effect)
                      local upper = root
                      local root = (ChangeTrapStateEffect.Create)()
                      local upper = root
                      do
                        do
                          local root = ((ChangeTrapStateEffect.Parallel).Create)()
                          root:Add("[trap 12 8]")
                          root:Add("[trap 12 9]")
                          root:Add("[trap 12 10]")
                          upper:Add(root)
                          upper:AddEffect(root)
                          local upper = effect
                          do
                            local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                            effect:SetCfgID(5105108)
                            root:AddEffect(effect)
                            local upper = effect
                            do
                              do
                                local effect = (EffectFactory.CreateThawEffect)()
                                effect:SetCfgID(5105109)
                                root:AddEffect(effect)
                                if upper then
                                  upper:AddEffect(root)
                                end
                                return composed
                              end
                            end
                          end
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [568] = function(args)
  -- function num : 0_49 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5205200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(5205201)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(520520100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
          effect:SetCfgID(5205202)
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(5205203)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [569] = function(args)
  -- function num : 0_50 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5305300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(5305301)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(530530100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
          effect:SetCfgID(5305302)
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(5305303)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [570] = function(args)
  -- function num : 0_51 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5405400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(5405401)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(540540100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(5405402)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 17 3]")
              root:Add("[trap 17 4]")
              root:Add("[trap 17 5]")
              root:Add("[trap 17 7]")
              root:Add("[trap 17 8]")
              root:Add("[trap 17 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(5405404)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(5405405)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [571] = function(args)
  -- function num : 0_52 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5505500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(5505501)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(550550100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(5505502)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 17 2]")
              root:Add("[trap 17 3]")
              root:Add("[trap 17 5]")
              root:Add("[trap 17 7]")
              root:Add("[trap 17 9]")
              root:Add("[trap 17 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(5505504)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(5505505)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [572] = function(args)
  -- function num : 0_53 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5605600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 2, "", 0)
    effect:SetCfgID(5605601)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 2, "", 0)
        effect:SetCfgID(560560100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(5605602)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 17 3]")
              root:Add("[trap 17 4]")
              root:Add("[trap 17 5]")
              root:Add("[trap 17 7]")
              root:Add("[trap 17 8]")
              root:Add("[trap 17 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(5605604)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(5605605)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [573] = function(args)
  -- function num : 0_54 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(5705700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 3, "", 0)
    effect:SetCfgID(5705701)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 3, "", 0)
        effect:SetCfgID(570570100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(5705702)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 17 2]")
              root:Add("[trap 17 3]")
              root:Add("[trap 17 5]")
              root:Add("[trap 17 7]")
              root:Add("[trap 17 9]")
              root:Add("[trap 17 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(5705704)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(5705705)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [574] = function(args)
  -- function num : 0_55 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6206200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(6206201)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(620620100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(6206202)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 20 2]")
              root:Add("[trap 20 3]")
              root:Add("[trap 20 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6206204)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6206205)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [575] = function(args)
  -- function num : 0_56 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6306300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(6306301)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(630630100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(6306302)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 20 2]")
              root:Add("[trap 20 3]")
              root:Add("[trap 20 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6306304)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6306305)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [576] = function(args)
  -- function num : 0_57 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6406400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(6406401)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(640640100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(6406402)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 21 2]")
              root:Add("[trap 21 3]")
              root:Add("[trap 21 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6406404)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6406405)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [577] = function(args)
  -- function num : 0_58 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6506500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(6506501)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(650650100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(6506502)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 21 2]")
              root:Add("[trap 21 3]")
              root:Add("[trap 21 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6506504)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6506505)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [578] = function(args)
  -- function num : 0_59 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6606600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(6606601)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(660660100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(6606602)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 22 2]")
              root:Add("[trap 22 3]")
              root:Add("[trap 22 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6606604)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6606605)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [579] = function(args)
  -- function num : 0_60 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6706700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(6706701)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(670670100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(6706702)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 22 2]")
              root:Add("[trap 22 3]")
              root:Add("[trap 22 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6706704)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6706705)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [580] = function(args)
  -- function num : 0_61 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6806800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(6806801)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(680680100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(6806802)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 23 2]")
              root:Add("[trap 23 3]")
              root:Add("[trap 23 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6806804)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6806805)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [581] = function(args)
  -- function num : 0_62 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(6906900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(6906901)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(690690100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(6906902)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 23 2]")
              root:Add("[trap 23 3]")
              root:Add("[trap 23 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(6906904)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(6906905)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [582] = function(args)
  -- function num : 0_63 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7007000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(7007001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(700700100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(7007002)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 24 3]")
              root:Add("[trap 24 4]")
              root:Add("[trap 24 5]")
              root:Add("[trap 24 7]")
              root:Add("[trap 24 8]")
              root:Add("[trap 24 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7007004)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7007005)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [583] = function(args)
  -- function num : 0_64 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7107100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(7107101)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(710710100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(7107102)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 24 2]")
              root:Add("[trap 24 3]")
              root:Add("[trap 24 5]")
              root:Add("[trap 24 7]")
              root:Add("[trap 24 9]")
              root:Add("[trap 24 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7107104)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7107105)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [584] = function(args)
  -- function num : 0_65 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7207200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 2, "", 0)
    effect:SetCfgID(7207201)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 2, "", 0)
        effect:SetCfgID(720720100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(7207202)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 24 3]")
              root:Add("[trap 24 4]")
              root:Add("[trap 24 5]")
              root:Add("[trap 24 7]")
              root:Add("[trap 24 8]")
              root:Add("[trap 24 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7207204)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7207205)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [585] = function(args)
  -- function num : 0_66 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7307300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 3, "", 0)
    effect:SetCfgID(7307301)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 3, "", 0)
        effect:SetCfgID(730730100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(7307302)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 24 2]")
              root:Add("[trap 24 3]")
              root:Add("[trap 24 5]")
              root:Add("[trap 24 7]")
              root:Add("[trap 24 9]")
              root:Add("[trap 24 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7307304)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7307305)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [586] = function(args)
  -- function num : 0_67 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7407400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(7407401)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(740740100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(7407402)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 25 2]")
              root:Add("[trap 25 3]")
              root:Add("[trap 25 4]")
              root:Add("[trap 25 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7407404)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7407405)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [587] = function(args)
  -- function num : 0_68 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7507500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(7507501)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(750750100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(7507502)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 25 2]")
              root:Add("[trap 25 3]")
              root:Add("[trap 25 4]")
              root:Add("[trap 25 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7507504)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7507505)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [588] = function(args)
  -- function num : 0_69 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7607600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(7607601)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(760760100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(7607602)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 26 2]")
              root:Add("[trap 26 3]")
              root:Add("[trap 26 4]")
              root:Add("[trap 26 5]")
              root:Add("[trap 26 6]")
              root:Add("[trap 26 7]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(7607604)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 26 8]")
                    root:Add("[trap 26 9]")
                    root:Add("[trap 26 10]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(7607606)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(7607607)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [589] = function(args)
  -- function num : 0_70 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7707700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(7707701)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(770770100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(7707702)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 26 2]")
              root:Add("[trap 26 3]")
              root:Add("[trap 26 4]")
              root:Add("[trap 26 5]")
              root:Add("[trap 26 6]")
              root:Add("[trap 26 7]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(7707704)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 26 8]")
                    root:Add("[trap 26 9]")
                    root:Add("[trap 26 10]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(7707706)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(7707707)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [590] = function(args)
  -- function num : 0_71 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7807800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(7807801)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(780780100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(7807802)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 27 3]")
              root:Add("[trap 27 4]")
              root:Add("[trap 27 5]")
              root:Add("[trap 27 7]")
              root:Add("[trap 27 8]")
              root:Add("[trap 27 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7807804)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7807805)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [591] = function(args)
  -- function num : 0_72 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(7907900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(7907901)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(790790100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(7907902)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 27 2]")
              root:Add("[trap 27 3]")
              root:Add("[trap 27 5]")
              root:Add("[trap 27 7]")
              root:Add("[trap 27 9]")
              root:Add("[trap 27 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(7907904)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(7907905)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [592] = function(args)
  -- function num : 0_73 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8008000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(8008001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(800800100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(8008002)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 28 2]")
              root:Add("[trap 28 3]")
              root:Add("[trap 28 4]")
              root:Add("[trap 28 5]")
              root:Add("[trap 28 6]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(8008004)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 28 7]")
                    root:Add("[trap 28 8]")
                    root:Add("[trap 28 9]")
                    root:Add("[trap 28 10]")
                    root:Add("[trap 28 11]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(8008006)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(8008007)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [593] = function(args)
  -- function num : 0_74 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8108100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(8108101)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(810810100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(8108102)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 28 2]")
              root:Add("[trap 28 3]")
              root:Add("[trap 28 4]")
              root:Add("[trap 28 5]")
              root:Add("[trap 28 6]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(8108104)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 28 7]")
                    root:Add("[trap 28 8]")
                    root:Add("[trap 28 9]")
                    root:Add("[trap 28 10]")
                    root:Add("[trap 28 11]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(8108106)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(8108107)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [594] = function(args)
  -- function num : 0_75 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8208200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(8208201)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(820820100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(8208202)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 29 2]")
              root:Add("[trap 29 3]")
              root:Add("[trap 29 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
                effect:SetCfgID(8208204)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 29 5]")
                    root:Add("[trap 29 6]")
                    root:Add("[trap 29 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(8208206)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(8208207)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [595] = function(args)
  -- function num : 0_76 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8308300)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(8308301)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(830830100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(8308302)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 29 2]")
              root:Add("[trap 29 3]")
              root:Add("[trap 29 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM07")
                effect:SetCfgID(8308304)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 29 5]")
                    root:Add("[trap 29 6]")
                    root:Add("[trap 29 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(8308306)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(8308307)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [596] = function(args)
  -- function num : 0_77 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8408400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(8408401)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(840840100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(8408402)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 32 2]")
              root:Add("[trap 32 3]")
              root:Add("[trap 32 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(8408404)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(8408405)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [597] = function(args)
  -- function num : 0_78 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8508500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(8508501)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(850850100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(8508502)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 32 2]")
              root:Add("[trap 32 3]")
              root:Add("[trap 32 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(8508504)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(8508505)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [598] = function(args)
  -- function num : 0_79 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8608600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(8608601)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(860860100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(8608602)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 33 2]")
              root:Add("[trap 33 3]")
              root:Add("[trap 33 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                effect:SetCfgID(8608604)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 33 5]")
                    root:Add("[trap 33 6]")
                    root:Add("[trap 33 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(8608606)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(8608607)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [599] = function(args)
  -- function num : 0_80 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8708700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(8708701)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(870870100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(8708702)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 33 2]")
              root:Add("[trap 33 3]")
              root:Add("[trap 33 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
                effect:SetCfgID(8708704)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Sequence).Create)()
                    root:Add("[trap 33 5]")
                    root:Add("[trap 33 6]")
                    root:Add("[trap 33 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(8708706)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(8708707)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [600] = function(args)
  -- function num : 0_81 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8808800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(8808801)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(880880100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(8808802)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 34 2]")
              root:Add("[trap 34 3]")
              root:Add("[trap 34 4]")
              root:Add("[trap 34 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(8808804)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(8808805)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [601] = function(args)
  -- function num : 0_82 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(8908900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(8908901)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(890890100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(8908902)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 34 2]")
              root:Add("[trap 34 3]")
              root:Add("[trap 34 4]")
              root:Add("[trap 34 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(8908904)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(8908905)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [602] = function(args)
  -- function num : 0_83 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9009000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(9009001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 0, "", 0)
        effect:SetCfgID(900900100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(9009002)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(9009003)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(9009004)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(9009005)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [603] = function(args)
  -- function num : 0_84 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9109100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0.25)
    effect:SetCfgID(9109101)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("nil", "loadState", 1, "", 0)
        effect:SetCfgID(910910100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(9109102)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(9109103)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(9109104)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(9109105)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [604] = function(args)
  -- function num : 0_85 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
      effect:SetCfgID(9209201)
      root:AddEffect(effect)
      if upper then
        upper:AddEffect(root)
      end
      return composed
    end
  end
end
, [605] = function(args)
  -- function num : 0_86 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
      effect:SetCfgID(9309301)
      root:AddEffect(effect)
      if upper then
        upper:AddEffect(root)
      end
      return composed
    end
  end
end
, [606] = function(args)
  -- function num : 0_87 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9409400)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 2, "", 0)
    effect:SetCfgID(9409401)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 2, "", 0)
        effect:SetCfgID(940940100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(9409402)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 27 3]")
              root:Add("[trap 27 4]")
              root:Add("[trap 27 5]")
              root:Add("[trap 27 7]")
              root:Add("[trap 27 8]")
              root:Add("[trap 27 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(9409404)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(9409405)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [607] = function(args)
  -- function num : 0_88 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9509500)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 3, "", 0)
    effect:SetCfgID(9509501)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 3, "", 0)
        effect:SetCfgID(950950100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(9509502)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Sequence).Create)()
              root:Add("[trap 27 2]")
              root:Add("[trap 27 3]")
              root:Add("[trap 27 5]")
              root:Add("[trap 27 7]")
              root:Add("[trap 27 9]")
              root:Add("[trap 27 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(9509504)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(9509505)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [608] = function(args)
  -- function num : 0_89 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9609600)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "SwitchState", 1, "", 0)
    effect:SetCfgID(9609600)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "loadState", 1, "", 0)
        effect:SetCfgID(960960000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(9609600)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(9609600)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(9609600)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(9609600)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [609] = function(args)
  -- function num : 0_90 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9709700)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "SwitchState", 0, "", 0)
    effect:SetCfgID(9709700)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "loadState", 0, "", 0)
        effect:SetCfgID(970970000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(9709700)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(9709700)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(9709700)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(9709700)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [610] = function(args)
  -- function num : 0_91 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9809800)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "SwitchState", 1, "", 0)
    effect:SetCfgID(9809800)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "loadState", 1, "", 0)
        effect:SetCfgID(980980000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(9809800)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(9809800)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(9809800)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(9809800)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [611] = function(args)
  -- function num : 0_92 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(9909900)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "SwitchState", 0, "", 0)
    effect:SetCfgID(9909900)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("1", "loadState", 0, "", 0)
        effect:SetCfgID(990990000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(9909900)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(9909900)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(9909900)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(9909900)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [612] = function(args)
  -- function num : 0_93 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1000000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1000000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(100000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1000000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1000000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1000000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1000000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [613] = function(args)
  -- function num : 0_94 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1010000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1010000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(101000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1010000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1010000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1010000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1010000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [614] = function(args)
  -- function num : 0_95 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1020000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1020000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(102000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1020000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 53 2]")
              root:Add("[trap 53 3]")
              root:Add("[trap 53 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1020000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 53 5]")
                    root:Add("[trap 53 6]")
                    root:Add("[trap 53 7]")
                    root:Add("[trap 53 8]")
                    root:Add("[trap 53 9]")
                    root:Add("[trap 53 10]")
                    root:Add("[trap 53 11]")
                    root:Add("[trap 53 12]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1020000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1020000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [615] = function(args)
  -- function num : 0_96 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1030000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1030000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(103000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1030000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 53 2]")
              root:Add("[trap 53 3]")
              root:Add("[trap 53 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1030000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 53 5]")
                    root:Add("[trap 53 6]")
                    root:Add("[trap 53 7]")
                    root:Add("[trap 53 8]")
                    root:Add("[trap 53 9]")
                    root:Add("[trap 53 10]")
                    root:Add("[trap 53 11]")
                    root:Add("[trap 53 12]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1030000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1030000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [616] = function(args)
  -- function num : 0_97 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1040000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1040000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(104000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(1040000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1040000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1040000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1040000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [617] = function(args)
  -- function num : 0_98 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1050000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1050000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(105000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(1050000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1050000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1050000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1050000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [618] = function(args)
  -- function num : 0_99 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1060000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1060000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(106000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1060000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 55 2]")
              root:Add("[trap 55 3]")
              root:Add("[trap 55 4]")
              root:Add("[trap 55 5]")
              root:Add("[trap 55 6]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1060000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 55 7]")
                    root:Add("[trap 55 8]")
                    root:Add("[trap 55 9]")
                    root:Add("[trap 55 10]")
                    root:Add("[trap 55 11]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1060000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1060000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [619] = function(args)
  -- function num : 0_100 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1070000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1070000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(107000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1070000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 55 2]")
              root:Add("[trap 55 3]")
              root:Add("[trap 55 4]")
              root:Add("[trap 55 5]")
              root:Add("[trap 55 6]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1070000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 55 7]")
                    root:Add("[trap 55 8]")
                    root:Add("[trap 55 9]")
                    root:Add("[trap 55 10]")
                    root:Add("[trap 55 11]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1070000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1070000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [620] = function(args)
  -- function num : 0_101 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1080000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1080000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(108000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1080000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 56 2]")
              root:Add("[trap 56 3]")
              root:Add("[trap 56 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1080000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 56 5]")
                    root:Add("[trap 56 6]")
                    root:Add("[trap 56 7]")
                    root:Add("[trap 56 8]")
                    root:Add("[trap 56 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1080000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1080000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [621] = function(args)
  -- function num : 0_102 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1090000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1090000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(109000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1090000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 56 2]")
              root:Add("[trap 56 3]")
              root:Add("[trap 56 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1090000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 56 5]")
                    root:Add("[trap 56 6]")
                    root:Add("[trap 56 7]")
                    root:Add("[trap 56 8]")
                    root:Add("[trap 56 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1090000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1090000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [622] = function(args)
  -- function num : 0_103 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1100000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1100001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(110000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1100002)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1100003)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1100004)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1100005)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [623] = function(args)
  -- function num : 0_104 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1110000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1110001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(111000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1110002)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1110003)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1110004)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1110005)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [624] = function(args)
  -- function num : 0_105 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1120000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1120001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(112000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1120004)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 58 5]")
              root:Add("[trap 58 6]")
              root:Add("[trap 58 7]")
              root:Add("[trap 58 8]")
              root:Add("[trap 58 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM08")
                effect:SetCfgID(1120006)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 58 10]")
                    root:Add("[trap 58 11]")
                    root:Add("[trap 58 12]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1120008)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1120009)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [625] = function(args)
  -- function num : 0_106 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1130000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1130001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(113000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1130004)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 58 5]")
              root:Add("[trap 58 6]")
              root:Add("[trap 58 7]")
              root:Add("[trap 58 8]")
              root:Add("[trap 58 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM08")
                effect:SetCfgID(1130006)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 58 10]")
                    root:Add("[trap 58 11]")
                    root:Add("[trap 58 12]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1130008)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1130009)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [626] = function(args)
  -- function num : 0_107 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1140000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1140001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(114000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(1140002)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 59 2]")
              root:Add("[trap 59 3]")
              root:Add("[trap 59 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
                effect:SetCfgID(1140004)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 59 5]")
                    root:Add("[trap 59 6]")
                    root:Add("[trap 59 7]")
                    root:Add("[trap 59 8]")
                    root:Add("[trap 59 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1140008)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1140009)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [627] = function(args)
  -- function num : 0_108 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1150000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1150001)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(115000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM06")
          effect:SetCfgID(1150002)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 59 2]")
              root:Add("[trap 59 3]")
              root:Add("[trap 59 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
                effect:SetCfgID(1150004)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 59 5]")
                    root:Add("[trap 59 6]")
                    root:Add("[trap 59 7]")
                    root:Add("[trap 59 8]")
                    root:Add("[trap 59 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1150008)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1150009)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [628] = function(args)
  -- function num : 0_109 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1160000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1160000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(116000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1160000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1160000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1160000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1160000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [629] = function(args)
  -- function num : 0_110 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1170000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1170000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(117000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1170000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1170000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1170000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1170000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [630] = function(args)
  -- function num : 0_111 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1180000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1180000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(118000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1180000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 61 2]")
              root:Add("[trap 61 3]")
              root:Add("[trap 61 4]")
              root:Add("[trap 61 5]")
              root:Add("[trap 61 6]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(1180000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 61 7]")
                    root:Add("[trap 61 8]")
                    root:Add("[trap 61 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1180000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1180000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [631] = function(args)
  -- function num : 0_112 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1190000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1190000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(119000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1190000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 61 2]")
              root:Add("[trap 61 3]")
              root:Add("[trap 61 4]")
              root:Add("[trap 61 5]")
              root:Add("[trap 61 6]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(1190000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 61 7]")
                    root:Add("[trap 61 8]")
                    root:Add("[trap 61 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1190000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1190000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [632] = function(args)
  -- function num : 0_113 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1200000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1200000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(120000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1200000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1200000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1200000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1200000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [633] = function(args)
  -- function num : 0_114 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1210000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1210000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(121000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1210000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1210000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1210000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1210000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [634] = function(args)
  -- function num : 0_115 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1220000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1220000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0.25)
        effect:SetCfgID(122000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1220000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 63 3]")
              root:Add("[trap 63 4]")
              root:Add("[trap 63 5]")
              root:Add("[trap 63 7]")
              root:Add("[trap 63 8]")
              root:Add("[trap 63 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1220000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1220000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [635] = function(args)
  -- function num : 0_116 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1230000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0.25)
    effect:SetCfgID(1230000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0.25)
        effect:SetCfgID(123000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1230000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 63 2]")
              root:Add("[trap 63 3]")
              root:Add("[trap 63 5]")
              root:Add("[trap 63 7]")
              root:Add("[trap 63 9]")
              root:Add("[trap 63 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1230000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1230000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [636] = function(args)
  -- function num : 0_117 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1240000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 2, "", 0)
    effect:SetCfgID(1240000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 2, "", 0)
        effect:SetCfgID(124000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1240000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 63 3]")
              root:Add("[trap 63 4]")
              root:Add("[trap 63 5]")
              root:Add("[trap 63 7]")
              root:Add("[trap 63 8]")
              root:Add("[trap 63 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1240000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1240000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [637] = function(args)
  -- function num : 0_118 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1250000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 3, "", 0)
    effect:SetCfgID(1250000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 3, "", 0)
        effect:SetCfgID(125000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1250000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 63 2]")
              root:Add("[trap 63 3]")
              root:Add("[trap 63 5]")
              root:Add("[trap 63 7]")
              root:Add("[trap 63 9]")
              root:Add("[trap 63 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1250000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1250000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [638] = function(args)
  -- function num : 0_119 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1260000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1260000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(126000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1260000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1260000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1260000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1260000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [639] = function(args)
  -- function num : 0_120 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1270000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1270000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(127000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1270000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1270000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1270000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1270000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [640] = function(args)
  -- function num : 0_121 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1280000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1280000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(128000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1280000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1280000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1280000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1280000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [641] = function(args)
  -- function num : 0_122 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1290000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1290000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(129000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1290000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1290000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1290000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1290000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [642] = function(args)
  -- function num : 0_123 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1300000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1300000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0.25)
        effect:SetCfgID(130000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1300000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 66 3]")
              root:Add("[trap 66 4]")
              root:Add("[trap 66 5]")
              root:Add("[trap 66 7]")
              root:Add("[trap 66 8]")
              root:Add("[trap 66 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1300000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1300000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [643] = function(args)
  -- function num : 0_124 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1310000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0.25)
    effect:SetCfgID(1310000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0.25)
        effect:SetCfgID(131000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1310000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 66 2]")
              root:Add("[trap 66 3]")
              root:Add("[trap 66 5]")
              root:Add("[trap 66 7]")
              root:Add("[trap 66 9]")
              root:Add("[trap 66 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1310000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1310000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [644] = function(args)
  -- function num : 0_125 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1320000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 2, "", 0.25)
    effect:SetCfgID(1320000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 2, "", 0.25)
        effect:SetCfgID(132000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1320000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 66 3]")
              root:Add("[trap 66 4]")
              root:Add("[trap 66 5]")
              root:Add("[trap 66 7]")
              root:Add("[trap 66 8]")
              root:Add("[trap 66 9]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1320000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1320000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [645] = function(args)
  -- function num : 0_126 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1330000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 3, "", 0.25)
    effect:SetCfgID(1330000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 3, "", 0.25)
        effect:SetCfgID(133000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1330000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 66 2]")
              root:Add("[trap 66 3]")
              root:Add("[trap 66 5]")
              root:Add("[trap 66 7]")
              root:Add("[trap 66 9]")
              root:Add("[trap 66 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                effect:SetCfgID(1330000)
                root:AddEffect(effect)
                local upper = effect
                do
                  do
                    local effect = (EffectFactory.CreateThawEffect)()
                    effect:SetCfgID(1330000)
                    root:AddEffect(effect)
                    if upper then
                      upper:AddEffect(root)
                    end
                    return composed
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [646] = function(args)
  -- function num : 0_127 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1340000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1340000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(134000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1340000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1340000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1340000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1340000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [647] = function(args)
  -- function num : 0_128 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1350000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1350000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(135000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1350000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1350000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1350000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1350000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [648] = function(args)
  -- function num : 0_129 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1360000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1360000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(136000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1360000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 68 2]")
              root:Add("[trap 68 3]")
              root:Add("[trap 68 4]")
              root:Add("[trap 68 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(1360000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 68 6]")
                    root:Add("[trap 68 7]")
                    root:Add("[trap 68 8]")
                    root:Add("[trap 68 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1360000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1360000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [649] = function(args)
  -- function num : 0_130 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1370000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1370000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(137000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1370000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 68 2]")
              root:Add("[trap 68 3]")
              root:Add("[trap 68 4]")
              root:Add("[trap 68 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(1370000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 68 6]")
                    root:Add("[trap 68 7]")
                    root:Add("[trap 68 8]")
                    root:Add("[trap 68 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1370000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1370000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [650] = function(args)
  -- function num : 0_131 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1380000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1380000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(138000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1380000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1380000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1380000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1380000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [651] = function(args)
  -- function num : 0_132 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1390000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1390000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(139000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1390000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1390000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1390000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1390000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [652] = function(args)
  -- function num : 0_133 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1400000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1400000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(140000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1400000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1400000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1400000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1400000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [653] = function(args)
  -- function num : 0_134 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1410000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1410000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(141000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1410000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1410000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1410000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1410000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [654] = function(args)
  -- function num : 0_135 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1420000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1420000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(142000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[trap 71 5]")
            root:Add("[trap 71 6]")
            root:Add("[trap 71 7]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
              effect:SetCfgID(1420000)
              root:AddEffect(effect)
              local upper = root
              local root = (ChangeTrapStateEffect.Create)()
              local upper = root
              do
                do
                  local root = ((ChangeTrapStateEffect.Parallel).Create)()
                  root:Add("[trap 71 2]")
                  root:Add("[trap 71 3]")
                  root:Add("[trap 71 4]")
                  upper:Add(root)
                  upper:AddEffect(root)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                    effect:SetCfgID(1420000)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(1420000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [655] = function(args)
  -- function num : 0_136 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1430000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1430000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(143000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = root
        local root = (ChangeTrapStateEffect.Create)()
        local upper = root
        do
          do
            local root = ((ChangeTrapStateEffect.Parallel).Create)()
            root:Add("[trap 71 5]")
            root:Add("[trap 71 6]")
            root:Add("[trap 71 7]")
            upper:Add(root)
            upper:AddEffect(root)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
              effect:SetCfgID(1430000)
              root:AddEffect(effect)
              local upper = root
              local root = (ChangeTrapStateEffect.Create)()
              local upper = root
              do
                do
                  local root = ((ChangeTrapStateEffect.Parallel).Create)()
                  root:Add("[trap 71 2]")
                  root:Add("[trap 71 3]")
                  root:Add("[trap 71 4]")
                  upper:Add(root)
                  upper:AddEffect(root)
                  local upper = effect
                  do
                    local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                    effect:SetCfgID(1430000)
                    root:AddEffect(effect)
                    local upper = effect
                    do
                      do
                        local effect = (EffectFactory.CreateThawEffect)()
                        effect:SetCfgID(1430000)
                        root:AddEffect(effect)
                        if upper then
                          upper:AddEffect(root)
                        end
                        return composed
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [656] = function(args)
  -- function num : 0_137 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1440000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1440000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(144000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1440000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1440000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1440000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1440000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [657] = function(args)
  -- function num : 0_138 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1450000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1450000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(145000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1450000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1450000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1450000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1450000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [658] = function(args)
  -- function num : 0_139 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1460000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1460000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(146000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1460000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 73 2]")
              root:Add("[trap 73 3]")
              root:Add("[trap 73 4]")
              root:Add("[trap 73 8]")
              root:Add("[trap 73 9]")
              root:Add("[trap 73 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(1460000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 73 5]")
                    root:Add("[trap 73 6]")
                    root:Add("[trap 73 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1460000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1460000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [659] = function(args)
  -- function num : 0_140 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1470000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1470000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(147000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1470000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 73 2]")
              root:Add("[trap 73 3]")
              root:Add("[trap 73 4]")
              root:Add("[trap 73 8]")
              root:Add("[trap 73 9]")
              root:Add("[trap 73 10]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
                effect:SetCfgID(1470000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 73 5]")
                    root:Add("[trap 73 6]")
                    root:Add("[trap 73 7]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1470000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1470000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [660] = function(args)
  -- function num : 0_141 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1480000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1480000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(148000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1480000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 74 2]")
              root:Add("[trap 74 3]")
              root:Add("[trap 74 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1480000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 74 5]")
                    root:Add("[trap 74 6]")
                    root:Add("[trap 74 7]")
                    root:Add("[trap 74 8]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1480000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1480000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [665] = function(args)
  -- function num : 0_142 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1490000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1490000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(149000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1490000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 74 2]")
              root:Add("[trap 74 3]")
              root:Add("[trap 74 4]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1490000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 74 5]")
                    root:Add("[trap 74 6]")
                    root:Add("[trap 74 7]")
                    root:Add("[trap 74 8]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1490000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1490000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [666] = function(args)
  -- function num : 0_143 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1500000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1500000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(150000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1500000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1500000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1500000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1500000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [667] = function(args)
  -- function num : 0_144 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1510000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1510000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(151000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1510000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1510000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1510000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1510000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [668] = function(args)
  -- function num : 0_145 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "SkateRecover", 0.25)
  effect:SetCfgID(1520001)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(152000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [669] = function(args)
  -- function num : 0_146 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "SkateBreak", 0.25)
  effect:SetCfgID(1530001)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(153000100)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [670] = function(args)
  -- function num : 0_147 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1540000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1540000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(154000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1540000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1540000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1540000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1540000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [671] = function(args)
  -- function num : 0_148 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1550000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1550000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(155000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1550000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1550000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1550000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1550000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [672] = function(args)
  -- function num : 0_149 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1560000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1560000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(156000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1560000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1560000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1560000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1560000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [673] = function(args)
  -- function num : 0_150 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1570000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1570000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(157000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1570000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1570000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1570000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1570000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [674] = function(args)
  -- function num : 0_151 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1580000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1580000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(158000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1580000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1580000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1580000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1580000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [675] = function(args)
  -- function num : 0_152 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1590000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1590000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(159000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM05")
          effect:SetCfgID(1590000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1590000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1590000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1590000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [676] = function(args)
  -- function num : 0_153 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1600100)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1600100)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(160010000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
          effect:SetCfgID(1600100)
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(1600100)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [677] = function(args)
  -- function num : 0_154 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1610200)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1610200)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(161020000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
          effect:SetCfgID(1610200)
          root:AddEffect(effect)
          local upper = effect
          do
            do
              local effect = (EffectFactory.CreateThawEffect)()
              effect:SetCfgID(1610200)
              root:AddEffect(effect)
              if upper then
                upper:AddEffect(root)
              end
              return composed
            end
          end
        end
      end
    end
  end
end
, [678] = function(args)
  -- function num : 0_155 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1620000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1620000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(162000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1620000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1620000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1620000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1620000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [679] = function(args)
  -- function num : 0_156 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1630000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1630000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(163000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1630000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1630000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1630000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1630000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [680] = function(args)
  -- function num : 0_157 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 0, "SwitchOff", 0.25)
  effect:SetCfgID(1640100)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(164010000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [681] = function(args)
  -- function num : 0_158 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "trapState", 1, "SwitchOn", 0.25)
  effect:SetCfgID(1650200)
  effect:SetParam("runoninit")
  local upper = effect
  do
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(165020000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        if upper then
          upper:AddEffect(root)
        end
        return composed
      end
    end
  end
end
, [682] = function(args)
  -- function num : 0_159 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1660000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1660000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(166000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1660000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1660000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1660000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1660000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [683] = function(args)
  -- function num : 0_160 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1670000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1670000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(167000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1670000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1670000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1670000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1670000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [684] = function(args)
  -- function num : 0_161 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1680000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1680000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(168000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1680000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1680000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1680000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1680000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [685] = function(args)
  -- function num : 0_162 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1690000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1690000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(169000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1690000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1690000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1690000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1690000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [686] = function(args)
  -- function num : 0_163 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1700000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0)
    effect:SetCfgID(1700000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(170000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1700000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 98 2]")
              root:Add("[trap 98 3]")
              root:Add("[trap 98 4]")
              root:Add("[trap 98 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1700000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 98 6]")
                    root:Add("[trap 98 7]")
                    root:Add("[trap 98 8]")
                    root:Add("[trap 98 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1700000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1700000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [687] = function(args)
  -- function num : 0_164 , upvalues : _ENV, ChangeTrapStateEffect
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1710000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1710000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(171000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1710000)
          root:AddEffect(effect)
          local upper = root
          local root = (ChangeTrapStateEffect.Create)()
          local upper = root
          do
            do
              local root = ((ChangeTrapStateEffect.Parallel).Create)()
              root:Add("[trap 98 2]")
              root:Add("[trap 98 3]")
              root:Add("[trap 98 4]")
              root:Add("[trap 98 5]")
              upper:Add(root)
              upper:AddEffect(root)
              local upper = effect
              do
                local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
                effect:SetCfgID(1710000)
                root:AddEffect(effect)
                local upper = root
                local root = (ChangeTrapStateEffect.Create)()
                local upper = root
                do
                  do
                    local root = ((ChangeTrapStateEffect.Parallel).Create)()
                    root:Add("[trap 98 6]")
                    root:Add("[trap 98 7]")
                    root:Add("[trap 98 8]")
                    root:Add("[trap 98 9]")
                    upper:Add(root)
                    upper:AddEffect(root)
                    local upper = effect
                    do
                      local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
                      effect:SetCfgID(1710000)
                      root:AddEffect(effect)
                      local upper = effect
                      do
                        do
                          local effect = (EffectFactory.CreateThawEffect)()
                          effect:SetCfgID(1710000)
                          root:AddEffect(effect)
                          if upper then
                            upper:AddEffect(root)
                          end
                          return composed
                        end
                      end
                    end
                  end
                end
              end
            end
          end
        end
      end
    end
  end
end
, [688] = function(args)
  -- function num : 0_165 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1720000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1720000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(172000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1720000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1720000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1720000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1720000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [689] = function(args)
  -- function num : 0_166 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1730000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1730000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(173000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM03")
          effect:SetCfgID(1730000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1730000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1730000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1730000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [690] = function(args)
  -- function num : 0_167 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1740000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1740000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(174000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1740000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1740000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1740000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1740000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [691] = function(args)
  -- function num : 0_168 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1750000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1750000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(175000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM04")
          effect:SetCfgID(1750000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1750000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1750000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1750000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [692] = function(args)
  -- function num : 0_169 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1760000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1760000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(176000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1760000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1760000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1760000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1760000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [693] = function(args)
  -- function num : 0_170 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1770000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1770000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(177000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1770000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1770000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1770000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1770000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [694] = function(args)
  -- function num : 0_171 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1780000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1780000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(178000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1780000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1780000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1780000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1780000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [695] = function(args)
  -- function num : 0_172 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1790000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1790000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(179000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1790000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1790000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1790000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1790000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [696] = function(args)
  -- function num : 0_173 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1800000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1800000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(180000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1800000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1800000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1800000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1800000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [697] = function(args)
  -- function num : 0_174 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1810000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1810000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(181000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1810000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1810000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1810000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1810000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [698] = function(args)
  -- function num : 0_175 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1820000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 0, "", 0.25)
    effect:SetCfgID(1820000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 0, "", 0)
        effect:SetCfgID(182000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1820000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1820000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1820000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1820000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [699] = function(args)
  -- function num : 0_176 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1830000)
    root:AddEffect(effect)
    local upper = effect
    local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "SwitchState", 1, "", 0)
    effect:SetCfgID(1830000)
    effect:SetParam("runoninit")
    local upper = effect
    do
      do
        local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "loadState", 1, "", 0)
        effect:SetCfgID(183000000)
        upper:SetParam("alternative", effect)
        root:AddEffect(effect)
        local upper = effect
        do
          local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM02")
          effect:SetCfgID(1830000)
          root:AddEffect(effect)
          local upper = effect
          do
            local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
            effect:SetCfgID(1830000)
            root:AddEffect(effect)
            local upper = effect
            do
              local effect = (EffectFactory.CreateAnimationEffect)("CM StateDrivenCamera1", "CM01")
              effect:SetCfgID(1830000)
              root:AddEffect(effect)
              local upper = effect
              do
                do
                  local effect = (EffectFactory.CreateThawEffect)()
                  effect:SetCfgID(1830000)
                  root:AddEffect(effect)
                  if upper then
                    upper:AddEffect(root)
                  end
                  return composed
                end
              end
            end
          end
        end
      end
    end
  end
end
, [700] = function(args)
  -- function num : 0_177 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1840000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "switchState", 0, "", 0)
      effect:SetCfgID(1840000)
      effect:SetParam("runoninit")
      root:AddEffect(effect)
      local upper = effect
      do
        do
          local effect = (EffectFactory.CreateThawEffect)()
          effect:SetCfgID(1840000)
          root:AddEffect(effect)
          if upper then
            upper:AddEffect(root)
          end
          return composed
        end
      end
    end
  end
end
, [701] = function(args)
  -- function num : 0_178 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    local effect = (EffectFactory.CreateFreezeEffect)()
    effect:SetCfgID(1850000)
    root:AddEffect(effect)
    local upper = effect
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "switchState", 1, "", 0)
      effect:SetCfgID(1850000)
      effect:SetParam("runoninit")
      root:AddEffect(effect)
      local upper = effect
      do
        local effect = (EffectFactory.CreateTrapStateChangeEffect)(args.traps, args.switches)
        effect:SetCfgID(1850000)
        root:AddEffect(effect)
        local upper = effect
        do
          do
            local effect = (EffectFactory.CreateThawEffect)()
            effect:SetCfgID(1850000)
            root:AddEffect(effect)
            if upper then
              upper:AddEffect(root)
            end
            return composed
          end
        end
      end
    end
  end
end
, [702] = function(args)
  -- function num : 0_179 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal1", 1, "TowerOn1", 1)
      effect:SetCfgID(1860000)
      effect:SetParam("runoninit")
      root:AddEffect(effect)
      if upper then
        upper:AddEffect(root)
      end
      return composed
    end
  end
end
, [703] = function(args)
  -- function num : 0_180 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal2", 1, "TowerOn2", 1)
      effect:SetCfgID(1870000)
      effect:SetParam("runoninit")
      root:AddEffect(effect)
      if upper then
        upper:AddEffect(root)
      end
      return composed
    end
  end
end
, [704] = function(args)
  -- function num : 0_181 , upvalues : _ENV
  local composed = nil
  local upper = root or composed
  local root = nil
  if not composed then
    composed = (EffectFactory.CreateComposedEffect)()
    root = composed
  else
    root = (EffectFactory.CreateComposedEffect)()
  end
  local upper = effect
  do
    do
      local effect = (EffectFactory.CreateAnimatorStateChangeEffect)("", "crystal3", 1, "TowerOn3", 1)
      effect:SetCfgID(1880000)
      effect:SetParam("runoninit")
      root:AddEffect(effect)
      if upper then
        upper:AddEffect(root)
      end
      return composed
    end
  end
end
}
return effects

